generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Organization {
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id         String   @id @default(uuid())
  name       String
  slug       String   @unique
  code       String   @unique
  address    String?
  phone      String?
  email      String?
  license_no String?
  logo_url   String?
  is_active  Boolean  @default(true)
  plan       String   @default("free") // free, starter, pro, enterprise
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  config   OrganizationConfig?
  branding OrganizationBranding?

  users                         User[]
  patients                      OPD_REG[]
  appointments                  appointments[]
  clinical_ehrs                 Clinical_EHR[]
  vital_signs                   vital_signs[]
  triage_results                triage_results[]
  lab_orders                    lab_orders[]
  lab_test_inventory            lab_test_inventory[]
  lab_panels                    LabPanel[]
  lab_staff                     lab_staff[]
  lab_audit_logs                lab_audit_logs[]
  pharmacy_medicine_master      pharmacy_medicine_master[]
  pharmacy_orders               pharmacy_orders[]
  pharmacy_sales_audit          pharmacy_sales_audit[]
  admissions                    admissions[]
  medical_notes                 medical_notes[]
  discharge_summaries           discharge_summaries[]
  beds                          beds[]
  wards                         wards[]
  invoices                      invoices[]
  invoice_items                 invoice_items[]
  payments                      payments[]
  payment_order_intents         PaymentOrderIntent[]
  charge_catalog                charge_catalog[]
  insurance_providers           insurance_providers[]
  insurance_policies            insurance_policies[]
  insurance_claims              insurance_claims[]
  system_audit_logs             system_audit_logs[]
  user_mfa_records              user_mfa[]
  patient_password_setup_tokens PatientPasswordSetupToken[]
  ai_health_assessments         AiHealthAssessment[]
  appointment_slots             AppointmentSlot[]
  departments                   Department[]
  prescription_templates        PrescriptionTemplate[]
  follow_ups                    FollowUp[]
  lab_sample_tracking           LabSampleTracking[]
  lab_reagent_inventory         LabReagentInventory[]
  pharmacy_suppliers            PharmacySupplier[]
  purchase_orders               PurchaseOrder[]
  goods_receipt_notes           GoodsReceiptNote[]
  pharmacy_returns              PharmacyReturn[]
  ward_stock                    WardStock[]
  ward_stock_transactions       WardStockTransaction[]
  bed_transfers                 BedTransfer[]
  diet_plans                    DietPlan[]
  ward_rounds                   WardRound[]
  nursing_tasks                 NursingTask[]
  patient_feedbacks             PatientFeedback[]
  cash_closures                 CashClosure[]
  refunds                       Refund[]
  // Phase 3 models
  nursing_notes                 NursingNote[]
  medication_administrations    MedicationAdministration[]
  shift_handovers               ShiftHandover[]
  employees                     Employee[]
  attendances                   Attendance[]
  leave_types                   LeaveType[]
  leave_requests                LeaveRequest[]
  shift_patterns                ShiftPattern[]
  shift_assignments             ShiftAssignment[]
  notifications                 Notification[]
  expense_categories            ExpenseCategory[]
  vendors                       Vendor[]
  expenses                      Expense[]
  tax_configs                   TaxConfig[]
  patient_deposits              PatientDeposit[]
  credit_notes                  CreditNote[]
  financial_periods             FinancialPeriod[]
  item_categories               ItemCategory[]
  item_masters                  ItemMaster[]
  item_vendors                  ItemVendor[]
  stores                        Store[]
  store_item_settings           StoreItemSetting[]
  item_batches                  ItemBatch[]
  store_stocks                  StoreStock[]
  inventory_movements           InventoryMovement[]
  purchase_requisitions         PurchaseRequisition[]
  indents                       Indent[]
  stock_issues                  StockIssue[]
  stock_transfers               StockTransfer[]
  stock_count_sessions          StockCountSession[]
  stock_adjustments             StockAdjustment[]
  bank_transactions             BankTransaction[]
  dunning_rules                 DunningRule[]
  dunning_logs                  DunningLog[]
  ipd_service_masters           IpdServiceMaster[]
  ipd_tariff_rates              IpdTariffRate[]
  ipd_packages                  IpdPackage[]
  ipd_admission_packages        IpdAdmissionPackage[]
  ipd_estimates                 IpdEstimate[]
  ipd_charge_postings           IpdChargePosting[]
  branches                      Branch[]
  // Admin panel dynamic configuration
  module_configs                ModuleConfig[]
  custom_roles                  Role[]
  document_templates            DocumentTemplate[]
  alert_rules                   AlertRule[]
  // Data import & archival
  data_import_jobs              DataImportJob[]
  archived_patient_records      ArchivedPatientRecord[]
  // Zealthix insurance integration
  zealthix_api_keys             ZealthixApiKey[]
  pill_reminders                PillReminder[]
  message_delivery_logs         MessageDeliveryLog[]
  whatsapp_incoming_msgs        WhatsAppIncomingMessage[]
  video_call_requests           VideoCallRequest[]
  // Phase 1 — Corporate & Pre-Authorization
  corporate_masters             CorporateMaster[]
  // Phase 2 — Payment Splits
  payment_splits                PaymentSplit[]
  // Phase 3 — EMR
  clinical_encounters           ClinicalEncounter[]
  patient_allergies             PatientAllergy[]
  // IPD Upgrade
  ipd_vitals                    IPDVitals[]
  nursing_assessments           NursingAssessment[]
  insurance_preauths            InsurancePreAuth[]
  admission_consultants         AdmissionConsultant[]
  // Finance: GST Compliance
  gst_invoice_register          GST_Invoice_Register[]
  gst_return_filings            GST_Return_Filing[]
  hsn_sac_master                HSN_SAC_Master[]
  // Finance: General Ledger & Tally Integration
  gl_accounts                   GL_Account[]
  gl_journal_entries            GL_JournalEntry[]
  gl_journal_lines              GL_JournalLine[]
  tally_exports                 TallyExport[]
  // Finance: Fixed Asset Management
  asset_categories              AssetCategory[]
  fixed_assets                  FixedAsset[]
  depreciation_entries          DepreciationEntry[]
  asset_transfers               AssetTransfer[]
  asset_maintenance             AssetMaintenance[]
  // Finance: Budget Management
  budget_masters                BudgetMaster[]
  budget_lines                  BudgetLine[]
  budget_revisions              BudgetRevision[]
  budget_alerts                 BudgetAlert[]

  // GAP 5 — 2-Hour Assessment Alerts
  nursing_assessment_alerts      NursingAssessmentAlert[]
  // GAP 6 — 14-Tab IPD EMR
  clinical_orders                ClinicalOrder[]
  physician_orders               PhysicianOrder[]
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id                             String                        @id @default(uuid())
  name                           String
  slug                           String                        @unique
  code                           String                        @unique
  address                        String?
  phone                          String?
  email                          String?
  license_no                     String?
  logo_url                       String?
  is_active                      Boolean                       @default(true)
  plan                           String                        @default("free")
  created_at                     DateTime                      @default(now())
  updated_at                     DateTime                      @updatedAt
  accreditation_body             String?
  accreditation_expiry           DateTime?
  accreditation_number           String?
  bed_capacity                   Int?
  established_year               Int?
  hospital_type                  String?
  latitude                       Float?
  longitude                      Float?
  registration_authority         String?
  registration_number            String?
  specialties                    String[]                      @default([])
  website                        String?
  gst_state_code                 String?
  organization_gstin             String?
  admission_bookings             AdmissionBooking[]
  appointment_waitlists          AppointmentWaitlist[]
  billing_order_sets             BillingOrderSet[]
  crm_campaigns                  CRMCampaign[]
  crm_leads                      CRMLead[]
  call_logs                      CallLog[]
  clinical_ehrs                  Clinical_EHR[]
  counselling_sessions           CounsellingSession[]
  discharge_clearances           DischargeClearance[]
  discount_otps                  DiscountOTP[]
  discount_schemes               DiscountScheme[]
  doctor_leaves                  DoctorLeave[]
  doctor_referrals               DoctorReferralNetwork[]
  financial_counsellings         FinancialCounselling[]
  narcotic_registers             NarcoticRegister[]
  patients                       OPD_REG[]
  patient_consents_ipd           PatientConsent_IPD[]
  patient_engagements            PatientEngagement[]
  patient_movements              PatientMovement[]
  registration_field_configs     RegistrationFieldConfig[]
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c
  active_medications             ActiveMedication[]
  admission_consultants          AdmissionConsultant[]
  admissions                     admissions[]
  ai_health_assessments          AiHealthAssessment[]
  alert_rules                    AlertRule[]
  appointment_slots              AppointmentSlot[]
  appointments                   appointments[]
  archived_patient_records       ArchivedPatientRecord[]
  asset_categories               AssetCategory[]
  asset_maintenance              AssetMaintenance[]
  asset_transfers                AssetTransfer[]
  attendances                    Attendance[]
  attendance_punches             AttendancePunch[]
  attendance_devices             AttendanceDevice[]
  attendance_regularizations     AttendanceRegularization[]
  attendance_policy              AttendancePolicy?
  bank_transactions              BankTransaction[]
  bed_transfers                  BedTransfer[]
  beds                           beds[]
  branches                       Branch[]
  budget_alerts                  BudgetAlert[]
  budget_lines                   BudgetLine[]
  budget_masters                 BudgetMaster[]
  budget_revisions               BudgetRevision[]
  cash_closures                  CashClosure[]
  charge_catalog                 charge_catalog[]
  clinical_encounters            ClinicalEncounter[]
  clinical_orders                ClinicalOrder[]
  corporate_masters              CorporateMaster[]
  credit_notes                   CreditNote[]
  data_import_jobs               DataImportJob[]
  departments                    Department[]
  depreciation_entries           DepreciationEntry[]
  diet_plans                     DietPlan[]
  discharge_summaries            discharge_summaries[]
  dispense_allocations           DispenseAllocation[]
  doctor_investigation_favorites DoctorInvestigationFavorite[]
  document_templates             DocumentTemplate[]
  dunning_logs                   DunningLog[]
  dunning_rules                  DunningRule[]
  employees                      Employee[]
  er_registrations               ERRegistration[]
  expense_categories             ExpenseCategory[]
  expenses                       Expense[]
  financial_periods              FinancialPeriod[]
  fixed_assets                   FixedAsset[]
  follow_ups                     FollowUp[]
  gl_accounts                    GL_Account[]
  gl_journal_entries             GL_JournalEntry[]
  gl_journal_lines               GL_JournalLine[]
  goods_receipt_notes            GoodsReceiptNote[]
  gst_invoice_register           GST_Invoice_Register[]
  gst_return_filings             GST_Return_Filing[]
  hsn_sac_master                 HSN_SAC_Master[]
  indents                        indents[]
  insurance_claims               insurance_claims[]
  insurance_policies             insurance_policies[]
  insurance_preauths             InsurancePreAuth[]
  insurance_providers            insurance_providers[]
  inventory_movements            inventory_movements[]
  invoice_items                  invoice_items[]
  invoices                       invoices[]
  ipd_admission_packages         IpdAdmissionPackage[]
  ipd_charge_postings            IpdChargePosting[]
  ipd_estimates                  IpdEstimate[]
  ipd_packages                   IpdPackage[]
  ipd_service_masters            IpdServiceMaster[]
  ipd_tariff_rates               IpdTariffRate[]
  ipd_vitals                     IPDVitals[]
  item_batches                   item_batches[]
  item_categories                item_categories[]
  item_master                    item_master[]
  lab_audit_logs                 lab_audit_logs[]
  lab_orders                     lab_orders[]
  lab_panels                     LabPanel[]
  lab_reagent_inventory          LabReagentInventory[]
  lab_sample_tracking            LabSampleTracking[]
  lab_staff                      lab_staff[]
  lab_test_inventory             lab_test_inventory[]
  leave_requests                 LeaveRequest[]
  leave_types                    LeaveType[]
  medical_notes                  medical_notes[]
  medication_administrations     MedicationAdministration[]
  message_delivery_logs          MessageDeliveryLog[]
  module_configs                 ModuleConfig[]
  notifications                  Notification[]
  nursing_assessment_alerts      NursingAssessmentAlert[]
  nursing_assessments            NursingAssessment[]
  nursing_notes                  NursingNote[]
  nursing_tasks                  NursingTask[]
  order_sets                     OrderSet[]
  branding                       OrganizationBranding?
  config                         OrganizationConfig?
  ot_rooms                       OTRoom[]
  ot_schedules                   OTSchedule[]
  patient_allergies              PatientAllergy[]
  patient_deposits               PatientDeposit[]
  patient_feedbacks              PatientFeedback[]
  patient_password_setup_tokens  PatientPasswordSetupToken[]
  payment_order_intents          PaymentOrderIntent[]
  payment_splits                 PaymentSplit[]
  payments                       payments[]
  pharmacy_inventory_movements   PharmacyInventoryMovement[]
  pharmacy_medicine_master       pharmacy_medicine_master[]
  pharmacy_orders                pharmacy_orders[]
  pharmacy_purchase_invoices     PharmacyPurchaseInvoice[]
  pharmacy_returns               PharmacyReturn[]
  pharmacy_sales_audit           pharmacy_sales_audit[]
  pharmacy_suppliers             PharmacySupplier[]
  physician_orders               PhysicianOrder[]
  pill_reminders                 PillReminder[]
  prescription_templates         PrescriptionTemplate[]
  purchase_orders                PurchaseOrder[]
  purchase_requisitions          purchase_requisitions[]
  referral_orders                ReferralOrder[]
  refunds                        Refund[]
  report_access_logs             ReportAccessLog[]
  report_jobs                    ReportJob[]
  report_presets                 ReportPreset[]
  report_schedules               ReportSchedule[]
  custom_roles                   Role[]
  shift_assignments              ShiftAssignment[]
  shift_swap_requests            ShiftSwapRequest[]
  shift_handovers                ShiftHandover[]
  shift_patterns                 ShiftPattern[]
  stock_adjustments              stock_adjustments[]
  stock_count_sessions           stock_count_sessions[]
  stock_issues                   stock_issues[]
  stock_transfers                stock_transfers[]
  store_stock                    store_stock[]
  stores                         stores[]
  surgery_masters                SurgeryMaster[]
  surgery_requests               SurgeryRequest[]
  system_audit_logs              system_audit_logs[]
  tally_exports                  TallyExport[]
  tax_configs                    TaxConfig[]
  triage_results                 triage_results[]
  user_mfa_records               user_mfa[]
  users                          User[]
  vendors                        Vendor[]
  video_call_requests            VideoCallRequest[]
  vital_signs                    vital_signs[]
  ward_rounds                    WardRound[]
  ward_stock                     WardStock[]
  ward_stock_transactions        WardStockTransaction[]
  wards                          wards[]
  whatsapp_incoming_msgs         WhatsAppIncomingMessage[]
  writeoffs                      Writeoff[]
  zealthix_api_keys              ZealthixApiKey[]

  @@map("organizations")
}

model Branch {
  id             String       @id @default(uuid())
  branch_name    String
  branch_code    String
  address        String?
  city           String?
  state          String?
  pincode        String?
  phone          String?
  email          String?
  is_main_branch Boolean      @default(false)
  is_active      Boolean      @default(true)
  latitude       Float?
  longitude      Float?
  organizationId String
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  created_at     DateTime @default(now())
  updated_at     DateTime @updatedAt

  organization Organization @relation(fields: [organizationId], references: [id])
  stores       Store[]
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])
  stores         stores[]
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([organizationId])
  @@map("branches")
}

model SubscriptionPlan {
  id                     String   @id @default(uuid())
  plan_name              String
  plan_code              String   @unique
  max_users              Int      @default(0)
  max_branches           Int      @default(0)
  max_patients_per_month Int?
  features               String[] @default([])
  price_monthly          Float    @default(0)
  price_yearly           Float    @default(0)
  is_active              Boolean  @default(true)
  created_at             DateTime @default(now())
  updated_at             DateTime @updatedAt

  @@map("subscription_plans")
}

model OrganizationConfig {
  id                            String       @id @default(uuid())
  organizationId                String       @unique
  uhid_prefix                   String       @default("AVN")
  timezone                      String       @default("Asia/Kolkata")
  currency                      String       @default("INR")
  date_format                   String       @default("DD/MM/YYYY")
  session_timeout               Int          @default(15)
  enable_whatsapp               Boolean      @default(false)
  enable_razorpay               Boolean      @default(false)
  enable_ai_triage              Boolean      @default(true)
  razorpay_key_id               String?
  razorpay_key_secret           String?
  smtp_host                     String?
  smtp_user                     String?
  smtp_pass                     String?
  whatsapp_api_token            String?
  whatsapp_phone_id             String?
  openai_key                    String?
  whatsapp_app_secret           String?
  whatsapp_webhook_verify_token String?
  sender_phone_number           String?
  sms_api_key                   String?
  sms_gateway_url               String?
  sms_sender_id                 String?
  smtp_port                     Int?
  smtp_secure                   String?
  tally_enabled                 Boolean      @default(false)
  tally_url                     String?
  tally_company                 String?
  tally_username                String?
  tally_password                String?
  tally_auto_sync               Boolean      @default(false)
  tally_last_sync_at            DateTime?
  tally_voucher_type_map        String?
  organization                  Organization @relation(fields: [organizationId], references: [id])

  @@map("organization_configs")
}

model OrganizationBranding {
  id               String       @id @default(uuid())
  organizationId   String       @unique
  primary_color    String       @default("#10b981")
  secondary_color  String       @default("#0f172a")
  logo_url         String?
  portal_title     String       @default("Hospital OS")
  portal_subtitle  String       @default("Management System")
  footer_text      String?
  accent_color     String       @default("#1e3a6e")
  footer_height    Int          @default(80)
  header_height    Int          @default(130)
  letterhead_url   String?
  signature_name   String?
  signature_title  String?
  tagline          String?
  terms_conditions String?
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@map("organization_brandings")
}

model ModuleConfig {
  id             String       @id @default(uuid())
  organizationId String
  module_key     String
  enabled        Boolean      @default(true)
  config_json    Json         @default("{}")
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, module_key])
  @@index([organizationId])
  @@map("module_configs")
}

model Role {
  id             String       @id @default(uuid())
  organizationId String
  name           String
  slug           String
  is_system      Boolean      @default(false)
  is_active      Boolean      @default(true)
  permissions    Json         @default("[]")
  data_scope     String       @default("own")
  description    String?
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, slug])
  @@index([organizationId])
  @@map("roles")
}

model Permission {
  id          String  @id @default(uuid())
  key         String  @unique
  module      String
  action      String
  label       String
  description String?

  @@map("permissions")
}

model DocumentTemplate {
  id             String       @id @default(uuid())
  organizationId String
  type           String
  name           String
  content_json   Json         @default("{}")
  is_default     Boolean      @default(false)
  is_active      Boolean      @default(true)
  created_by     String?
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, type, name])
  @@index([organizationId])
  @@map("document_templates")
}

model AlertRule {
  id             String       @id @default(uuid())
  organizationId String
  category       String
  trigger_key    String
  name           String
  enabled        Boolean      @default(true)
  channels       Json         @default("[\"in_app\"]")
  recipients     Json         @default("{}")
  threshold      Json?
  escalation     Json?
  template       String?
  quiet_hours    Json?
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("alert_rules")
}

model SuperAdmin {
  id         String   @id @default(uuid())
  email      String   @unique
  password   String
  name       String
  is_active  Boolean  @default(true)
  created_at DateTime @default(now())

  @@map("super_admins")
}

model User {
  id                       String                  @id @default(uuid())
  username                 String                  @unique
  password                 String
  role                     String
  name                     String?
  specialty                String?
  email                    String?
  phone                    String?
  assigned_ward_id         Int?
  organizationId           String
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  is_active                Boolean  @default(true)
  working_hours            String   @default("09:00-17:00")
  slot_duration            Int      @default(20)
  consultation_fee         Float    @default(500)
  follow_up_fee            Float    @default(300)
  max_overbooking_per_slot Int      @default(0) // extra patients allowed beyond slot capacity
  gender                   String?  // Male | Female | Other
  working_days             String?  // e.g. "Mon-Sat" or "Mon,Wed,Fri"
  max_patients_per_day     Int?     // daily cap per doctor
  createdAt                DateTime @default(now())

  // GAP 3 — Doctor Group Hierarchy — Senior/Junior Access
  supervisor_id   String? // Senior doctor ID
  doctor_group_id String? // Group identifier
  is_group_lead   Boolean @default(false)

  // Non-Doctor Staff fields (section 3.2)
  employee_code   String?  // Internal employee ID e.g. EMP-0042 (Preferred)
  designation     String?  // Job title e.g. Senior Nurse (Preferred)
  department      String?  // Department name e.g. Cardiology, Lab, Pharmacy (Yes for non-doctor)
  date_of_joining DateTime? // DD/MM/YYYY — required for HR module

  organization      Organization      @relation(fields: [organizationId], references: [id])
  appointment_slots AppointmentSlot[]
  employee_profile  Employee?         // reverse link to HR Employee record
  incharge_stores   Store[]
  transfers_dispatched StockTransfer[]  @relation("TransferDispatchUser")
  transfers_received   StockTransfer[]  @relation("TransferReceiveUser")
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  is_active                Boolean                 @default(true)
  createdAt                DateTime                @default(now())
  slot_duration            Int                     @default(20)
  working_hours            String                  @default("09:00-17:00")
  consultation_fee         Float                   @default(500)
  doctor_registration_no   String?
  follow_up_fee            Float                   @default(300)
  qualifications           String?
  doctor_group_id          String?
  is_group_lead            Boolean                 @default(false)
  supervisor_id            String?
  max_overbooking_per_slot Int                     @default(0)
  gender                   String?
  max_patients_per_day     Int?
  working_days             String?
  date_of_joining          DateTime?
  department               String?
  designation              String?
  employee_code            String?
  appointment_slots        AppointmentSlot[]
  employee_profile         Employee?
  indents                  indents[]
  inventory_movements      inventory_movements[]
  purchase_requisitions    purchase_requisitions[]
  report_access_logs       ReportAccessLog[]
  requested_reports        ReportJob[]             @relation("ReportJobRequestedBy")
  report_presets           ReportPreset[]
  report_schedules         ReportSchedule[]        @relation("ReportScheduleOwner")
  stock_issues             stock_issues[]
  stores                   stores[]
  organization             Organization            @relation(fields: [organizationId], references: [id])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([organizationId])
  @@index([doctor_group_id])
  @@map("users")
}

model OPD_REG {
  id                         Int                         @id @default(autoincrement())
  patient_id                 String                      @unique
  full_name                  String
  age                        String?
  gender                     String?
  phone                      String?
  email                      String?
  address                    String?
  department                 String?
  aadhar_card                String?
  emergency_contact_name     String?
  emergency_contact_phone    String?
  blood_group                String?
  date_of_birth              String?
  organizationId             String
  password                   String?
  created_at                 DateTime                    @default(now())
  allergies                  String?
  chronic_conditions         String?
  emergency_contact_relation String?
  profile_photo_url          String?
  registration_consent       Boolean                     @default(false)
  abha_address               String?
  abha_number                String?
  archived_at                DateTime?
  employee_id                String?
  is_archived                Boolean                     @default(false)
  pan_number                 String?
  registration_number        String?
  corporate_card_number      String?
  corporate_id               String?
  patient_type               String                      @default("cash")
  age_in_days                Int?
  distance_from_hospital_km  Float?
  frro_number                String?
  is_secondary               Boolean                     @default(false)
  is_senior_citizen          Boolean                     @default(false)
  is_vip                     Boolean                     @default(false)
  lead_source                String?
  marital_status             String?
  merged_at                  DateTime?
  merged_by                  String?
  merged_into                String?
  parent_patient_id          String?
  patient_category           String?
  preferred_language         String?                     @default("en")
  race                       String?
  registration_form_url      String?
  registration_remarks       String?
  title                      String?
  corporate                  CorporateMaster?            @relation(fields: [corporate_id], references: [id])
  organization               Organization                @relation(fields: [organizationId], references: [id])
  admissions                 admissions[]
  appointments               appointments[]
  clinical_encounters        ClinicalEncounter[]
  insurance_policies         insurance_policies[]
  invoices                   invoices[]
  patient_allergies          PatientAllergy[]
  feedbacks                  PatientFeedback[]
  password_setup_tokens      PatientPasswordSetupToken[]
  pill_reminders             PillReminder[]
  video_call_requests        VideoCallRequest[]
  whatsapp_logs              whatsapp_log[]

  @@index([organizationId])
  @@index([organizationId, is_archived])
  @@index([created_at])
  @@index([organizationId, created_at])
  @@index([patient_type])
}

model appointments {
  id                    Int          @id @default(autoincrement())
  appointment_id        String       @unique
  patient_id            String
  doctor_name           String?
  doctor_id             String?
  department            String?
  status                String       @default("Pending")
  reason_for_visit      String?
  cancellation_reason   String?
  follow_up_date        DateTime?
  organizationId        String
  queue_token           Int?
  checked_in_at         DateTime?
  appointment_date      DateTime     @default(now())
  reminder24Sent        Boolean      @default(false)
  slot_id               String?      @unique
  called_at             DateTime?
  booking_channel       String?      @default("walk_in")
  is_pav                Boolean      @default(false)
  pav_sms_sent          Boolean      @default(false)
  payment_mode          String       @default("ONLINE")
  appointment_type      String       @default("NEW_OPD")
  parent_appointment_id String?
  payment_status        String       @default("PENDING")
  priority              String?      @default("Regular")
  recurrence_rule       Json?
  organization          Organization @relation(fields: [organizationId], references: [id])
  patient               OPD_REG      @relation(fields: [patient_id], references: [patient_id])

  @@index([organizationId])
  @@index([status])
  @@index([patient_id])
  @@index([appointment_date])
  @@index([doctor_id])
  @@index([booking_channel])
  @@index([appointment_type])
  @@index([payment_mode])
}

model Clinical_EHR {
  appointment_id String       @id
  patient_id     String
  doctor_name    String?
  diagnosis      String?
  doctor_notes   String?
  organizationId String
  created_at     DateTime     @default(now())
  archived_at    DateTime?
  is_archived    Boolean      @default(false)
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([organizationId, is_archived])
}

model VideoCallRequest {
  id               String       @id @default(uuid())
  patient_id       String
  doctor_id        String
  request_date     DateTime     @default(now())
  status           String       @default("Pending")
  scheduled_at     DateTime?
  meet_link        String?
  reason           String?
  rejection_reason String?
  organizationId   String
  organization     Organization @relation(fields: [organizationId], references: [id])
  patient          OPD_REG      @relation(fields: [patient_id], references: [patient_id])

  @@index([organizationId])
  @@index([patient_id])
  @@index([doctor_id])
  @@map("video_call_requests")
}

model lab_orders {
  id                     Int          @id @default(autoincrement())
  barcode                String       @unique
  patient_id             String
  doctor_id              String
  test_type              String
  status                 String
  result_value           String?
  report_url             String?
  assigned_technician_id String?
  is_critical            Boolean      @default(false)
  critical_notified_at   DateTime?
  organizationId         String
  created_at             DateTime     @default(now())
  technician_remarks     String?
  archived_at            DateTime?
  is_archived            Boolean      @default(false)
  organization           Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([status])
  @@index([patient_id])
  @@index([created_at])
  @@index([organizationId, is_archived])
}

model lab_test_inventory {
  id                    Int            @id @default(autoincrement())
  test_name             String
  price                 Float
  is_available          Boolean        @default(true)
  normal_range_min      Float?
  normal_range_max      Float?
  unit                  String?
  category              String?
  sample_type           String?
  organizationId        String
  hsn_sac_code          String?
  tax_rate              Float?         @default(0)
  requires_prescription Boolean        @default(false)
  critical_value_high   Float?
  critical_value_low    Float?
  test_code             String?
  turnaround_time       String?
  panel_tests           LabPanelTest[]
  organization          Organization   @relation(fields: [organizationId], references: [id])

  @@unique([test_name, organizationId])
  @@index([organizationId])
  @@map("lab_test_inventory")
}

model LabPanel {
  id             Int            @id @default(autoincrement())
  panel_name     String
  panel_code     String?
  description    String?
  panel_price    Float
  is_active      Boolean        @default(true)
  organizationId String
  created_at     DateTime       @default(now())
  updated_at     DateTime       @updatedAt
  panel_tests    LabPanelTest[]
  organization   Organization   @relation(fields: [organizationId], references: [id])

  @@unique([panel_name, organizationId])
  @@index([organizationId])
  @@map("lab_panels")
}

model LabPanelTest {
  id         Int                @id @default(autoincrement())
  panel_id   Int
  test_id    Int
  test_name  String
  sort_order Int                @default(0)
  panel      LabPanel           @relation(fields: [panel_id], references: [id], onDelete: Cascade)
  test       lab_test_inventory @relation(fields: [test_id], references: [id], onDelete: Cascade)

  @@unique([panel_id, test_id])
  @@map("lab_panel_tests")
}

model lab_staff {
  id             Int          @id @default(autoincrement())
  name           String
  role           String
  is_on_shift    Boolean      @default(false)
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model lab_audit_logs {
  id             Int           @id @default(autoincrement())
  action_type    String
  barcode        String
  details        String?
  organizationId String?
  created_at     DateTime      @default(now())
  organization   Organization? @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model pharmacy_medicine_master {
  id                     Int                           @id @default(autoincrement())
  brand_name             String
  generic_name           String?
  category               String?
  manufacturer           String?
  price_per_unit         Float                         @default(0)
  min_threshold          Int                           @default(10)
  organizationId         String
  hsn_sac_code           String?
  tax_rate               Float?                        @default(0)
  form                   String?
  gst_percent            Float                         @default(0)
  is_active              Boolean                       @default(true)
  mrp                    Float                         @default(0)
  purchase_price         Float                         @default(0)
  selling_price          Float                         @default(0)
  strength               String?
  drug_schedule          String?
  is_narcotic            Boolean                       @default(false)
  is_refrigerated        Boolean                       @default(false)
  ndc_code               String?
  batches                pharmacy_batch_inventory[]
  inventory_movements    PharmacyInventoryMovement[]
  organization           Organization                  @relation(fields: [organizationId], references: [id])
  order_items            pharmacy_order_items[]
  purchase_invoice_lines PharmacyPurchaseInvoiceLine[]
  ward_stock             WardStock[]

  @@unique([brand_name, organizationId])
  @@index([organizationId])
}

model pharmacy_batch_inventory {
  id                   Int                         @id @default(autoincrement())
  medicine_id          Int
  batch_no             String
  current_stock        Int
  expiry_date          DateTime
  rack_location        String?
  cost_price           Float?
  manufacture_date     DateTime?
  supplier_name        String?
  grn_id               Int?
  mrp                  Float?
  actual_cost          Float?
  vendor_id            Int?
  is_quarantined       Boolean                     @default(false)
  dispense_allocations DispenseAllocation[]
  grn                  GoodsReceiptNote?           @relation(fields: [grn_id], references: [id])
  medicine             pharmacy_medicine_master    @relation(fields: [medicine_id], references: [id])
  inventory_movements  PharmacyInventoryMovement[]

  @@unique([medicine_id, batch_no])
  @@index([grn_id])
  @@index([vendor_id])
  @@index([expiry_date])
}

model pharmacy_sales_audit {
  id             Int           @id @default(autoincrement())
  action_type    String
  medicine_name  String?
  batch_no       String?
  quantity_sold  Int
  details        String?
  organizationId String?
  created_at     DateTime      @default(now())
  organization   Organization? @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model pharmacy_orders {
  id                    Int                    @id @default(autoincrement())
  patient_id            String
  doctor_id             String
  total_amount          Float                  @default(0.0)
  status                String                 @default("Pending")
  total_items_requested Int?
  items_dispensed       Int?
  items_missing         Int?
  organizationId        String
  created_at            DateTime               @default(now())
  admission_id          String?
  is_ipd_linked         Boolean                @default(false)
  invoice_id            Int?
  verification_notes    String?
  verified_at           DateTime?
  verified_by           String?
  items                 pharmacy_order_items[]
  invoice               invoices?              @relation(fields: [invoice_id], references: [id])
  organization          Organization           @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([invoice_id])
}

model NarcoticRegister {
  id               String       @id @default(uuid())
  organizationId   String
  drug_name        String
  batch_no         String?
  patient_id       String?
  patient_name     String?
  prescriber_id    String?
  prescriber_name  String?
  witness_name     String?
  quantity_in      Float        @default(0)
  quantity_out     Float        @default(0)
  balance          Float        @default(0)
  transaction_type String
  notes            String?
  created_at       DateTime     @default(now())
  medicine_id      Int?
  batch_id         Int?
  source_type      String?
  source_id        String?
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([drug_name])
  @@index([medicine_id])
  @@index([source_type])
}

model pharmacy_order_items {
  id                   Int                       @id @default(autoincrement())
  order_id             Int
  medicine_name        String
  quantity_requested   Int
  quantity_dispensed   Int?
  unit_price           Float?
  total_price          Float?
  batch_id             String?
  status               String                    @default("Pending")
  medicine_id          Int?
  hsn_sac_code         String?
  tax_amount           Float?                    @default(0)
  tax_rate             Float?                    @default(0)
  dispense_allocations DispenseAllocation[]
  medicine             pharmacy_medicine_master? @relation(fields: [medicine_id], references: [id])
  order                pharmacy_orders           @relation(fields: [order_id], references: [id])

  @@index([medicine_id])
}

model admissions {
  admission_id              String                 @id @default(uuid())
  patient_id                String
  bed_id                    String?
  ward_id                   Int?
  status                    String
  diagnosis                 String?
  doctor_name               String?
  organizationId            String
  admission_date            DateTime               @default(now())
  discharge_date            DateTime?
  admission_type            String?
  archived_at               DateTime?
  case_fir_number           String?
  case_injury_date          DateTime?
  case_is_police_report     Boolean                @default(false)
  case_is_rta               Boolean                @default(false)
  case_is_substance_abuse   Boolean                @default(false)
  case_is_test_conducted    Boolean                @default(false)
  consent_signature_url     String?
  id_cards_url              String?
  is_archived               Boolean                @default(false)
  is_death                  Boolean                @default(false)
  line_of_treatment         String?
  other_ailments            String?
  past_ailment_duration     String?
  past_ailments             String?
  surgery_requested         String?
  admission_category        String?
  admission_source          String?
  allergy_alerts            Json?
  attending_doctor_id       String?
  code_status               String?
  discharge_checklist       Json?
  discharge_disposition     String?
  discharge_instructions    String?
  discharge_type            String?
  expected_discharge_date   DateTime?
  fall_risk_score           Int?
  fit_for_discharge_at      DateTime?
  fit_for_discharge_by      String?
  isolation_type            String?
  medication_reconciliation Json?
  news_score_latest         Int?
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  attending_doctor_id       String? // FK to doctor user
  admission_source          String? // OPD | Emergency | Transfer | DirectAdmit
  discharge_type            String? // Normal | LAMA | DAMA | Absconded | Death | Transfer
  discharge_disposition     String? // home, SNF, rehab, etc.
  patient_class             String? // General | SemiPrivate | Private | Suite | ICU | NICU | PICU
  primary_diagnosis_icd     String? // ICD-10 code
  secondary_diagnoses       Json? // array of ICD-10 codes
  isolation_type            String? // None | Contact | Droplet | Airborne | Reverse
  medication_reconciliation Json? // discharge meds reconciliation
  discharge_instructions    String? // patient instructions
  allergy_alerts            Json? // pulled from patient master
  version                   Int       @default(0)
  billing_category          String?   @default("General") // General, Semi-Private, Private, Deluxe, VIP

  organization           Organization           @relation(fields: [organizationId], references: [id])
  patient                OPD_REG                @relation(fields: [patient_id], references: [patient_id])
  bed                    beds?                  @relation(fields: [bed_id], references: [bed_id])
  ward                   wards?                 @relation(fields: [ward_id], references: [ward_id])
  medical_notes          medical_notes[]
  summaries              discharge_summaries[]
  invoices               invoices[]
  insurance_claims       insurance_claims[]
  bed_transfers          BedTransfer[]
  diet_plans             DietPlan[]
  ward_rounds            WardRound[]
  nursing_tasks          NursingTask[]
  ipd_admission_packages IpdAdmissionPackage[]
  ipd_vitals             IPDVitals[]
  nursing_assessments    NursingAssessment[]
  admission_consultants  AdmissionConsultant[]
  // Finance: GST Compliance
  gst_invoice_register   GST_Invoice_Register[]
  gst_return_filings     GST_Return_Filing[]
  hsn_sac_master         HSN_SAC_Master[]
  // Finance: General Ledger & Tally Integration
  gl_accounts            GL_Account[]
  gl_journal_entries     GL_JournalEntry[]
  gl_journal_lines       GL_JournalLine[]
  tally_exports          TallyExport[]
  insurance_preauths     InsurancePreAuth[]
  inventory_movements    InventoryMovement[]
  indents                Indent[]
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  patient_class             String?
  pressure_ulcer_risk       Int?
  primary_diagnosis_icd     String?
  secondary_diagnoses       Json?
  version                   Int                    @default(0)
  billing_category          String?                @default("General")
  admission_consultants     AdmissionConsultant[]
  bed                       beds?                  @relation(fields: [bed_id], references: [bed_id])
  organization              Organization           @relation(fields: [organizationId], references: [id])
  patient                   OPD_REG                @relation(fields: [patient_id], references: [patient_id])
  ward                      wards?                 @relation(fields: [ward_id], references: [ward_id])
  bed_transfers             BedTransfer[]
  diet_plans                DietPlan[]
  summaries                 discharge_summaries[]
  gl_accounts               GL_Account[]
  gl_journal_entries        GL_JournalEntry[]
  gl_journal_lines          GL_JournalLine[]
  gst_invoice_register      GST_Invoice_Register[]
  gst_return_filings        GST_Return_Filing[]
  hsn_sac_master            HSN_SAC_Master[]
  insurance_claims          insurance_claims[]
  insurance_preauths        InsurancePreAuth[]
  invoices                  invoices[]
  ipd_admission_packages    IpdAdmissionPackage[]
  ipd_vitals                IPDVitals[]
  medical_notes             medical_notes[]
  nursing_assessments       NursingAssessment[]
  nursing_tasks             NursingTask[]
  tally_exports             TallyExport[]
  ward_rounds               WardRound[]
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([organizationId])
  @@index([status])
  @@index([patient_id])
  @@index([admission_date])
  @@index([organizationId, is_archived])
}

model medical_notes {
  id             Int          @id @default(autoincrement())
  admission_id   String
  note_type      String
  details        String
  organizationId String
  created_at     DateTime     @default(now())
  admission      admissions   @relation(fields: [admission_id], references: [admission_id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model discharge_summaries {
  id                Int          @id @default(autoincrement())
  admission_id      String
  patient_name      String?
  generated_summary String
  organizationId    String
  created_at        DateTime     @default(now())
  admission         admissions   @relation(fields: [admission_id], references: [admission_id])
  organization      Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model beds {
  bed_id                String       @id
  ward_id               Int?
  status                String?      @default("Available")
  organizationId        String
  bed_category          String?
  pricing_tier          String?
  cleaning_completed_at DateTime?
  cleaning_started_at   DateTime?
  is_isolation          Boolean      @default(false)
  last_occupied_by      String?
  bed_name              String?
  admissions            admissions[]
  organization          Organization @relation(fields: [organizationId], references: [id])
  wards                 wards?       @relation(fields: [ward_id], references: [ward_id], onDelete: NoAction, onUpdate: NoAction)

  @@index([organizationId])
  @@index([organizationId, status])
}

model wards {
  ward_id        Int          @id @default(autoincrement())
  ward_name      String
  ward_type      String
  cost_per_day   Decimal?     @default(0)
  nursing_charge Decimal?     @default(0)
  organizationId String
  department_id  String?
  floor_number   String?
  is_active      Boolean      @default(true)
  admissions     admissions[]
  beds           beds[]
  ward_stock     WardStock[]
  department     Department?  @relation(fields: [department_id], references: [id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model invoices {
  id                       Int                  @id @default(autoincrement())
  invoice_number           String               @unique
  patient_id               String
  admission_id             String?
  invoice_type             String               @default("OPD")
  total_amount             Decimal              @default(0)
  total_discount           Decimal              @default(0)
  net_amount               Decimal              @default(0)
  paid_amount              Decimal              @default(0)
  balance_due              Decimal              @default(0)
  status                   String               @default("Draft")
  notes                    String?
  organizationId           String
  created_at               DateTime             @default(now())
  updated_at               DateTime             @updatedAt
  finalized_at             DateTime?
  archived_at              DateTime?
  is_archived              Boolean              @default(false)
  approved_at              DateTime?
  approved_by              String?
  cgst_amount              Decimal              @default(0)
  estimate_id              Int?
  igst_amount              Decimal              @default(0)
  is_inter_state           Boolean              @default(false)
  sgst_amount              Decimal              @default(0)
  total_tax                Decimal              @default(0)
  billing_patient_type     String               @default("cash")
  concession_amount        Decimal              @default(0)
  concession_approved_by   String?
  concession_reason        String?
  corporate_id             String?
  corporate_invoice_number String?
  corporate_payable        Decimal              @default(0)
  patient_payable          Decimal              @default(0)
  pre_auth_id              String?
  tpa_claim_number         String?
  tpa_claim_status         String               @default("not_submitted")
  tpa_payable              Decimal              @default(0)
  tpa_provider_id          Int?
  tpa_settled_amount       Decimal              @default(0)
  version                  Int                  @default(1)
  addendum_reason          String?
  is_addendum              Boolean              @default(false)
  parent_invoice_id        String?
  cancelled_at             DateTime?
  cancelled_by             String?
  cancellation_reason      String?
  doctor_id                String?
  doctor_name              String?
  credit_notes             CreditNote[]
  insurance_claims         insurance_claims[]
  items                    invoice_items[]
  admission                admissions?          @relation(fields: [admission_id], references: [admission_id])
  organization             Organization         @relation(fields: [organizationId], references: [id])
  patient                  OPD_REG              @relation(fields: [patient_id], references: [patient_id])
  payment_order_intents    PaymentOrderIntent[]
  payment_splits           PaymentSplit[]
  payments                 payments[]
  pharmacy_orders          pharmacy_orders[]
  writeoffs                Writeoff[]

  @@index([organizationId])
  @@index([status])
  @@index([patient_id])
  @@index([billing_patient_type])
  @@index([created_at])
  @@index([organizationId, is_archived])
}

model invoice_items {
  id               Int          @id @default(autoincrement())
  invoice_id       Int
  department       String
  description      String
  quantity         Int          @default(1)
  unit_price       Decimal
  total_price      Decimal
  discount         Decimal      @default(0)
  net_price        Decimal
  ref_id           String?
  created_at       DateTime     @default(now())
  organizationId   String
  hsn_sac_code     String?
  service_category String?
  tax_amount       Decimal      @default(0)
  tax_rate         Decimal      @default(0)
  invoice          invoices     @relation(fields: [invoice_id], references: [id])
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([invoice_id])
  @@index([organizationId])
  @@index([department])
}

model payments {
  id                   Int          @id @default(autoincrement())
  receipt_number       String       @unique
  invoice_id           Int
  amount               Decimal
  payment_method       String
  payment_type         String
  razorpay_order_id    String?
  razorpay_payment_id  String?
  status               String       @default("Completed")
  notes                String?
  created_at           DateTime     @default(now())
  organizationId       String
  reference            String?
  transaction_group_id String?
  version              Int          @default(1)
  payer_pan_number     String?
  payer_pan_name       String?
  invoice              invoices     @relation(fields: [invoice_id], references: [id])
  organization         Organization @relation(fields: [organizationId], references: [id])

  @@index([invoice_id])
  @@index([organizationId])
  @@index([status])
  @@index([transaction_group_id])
}

model PaymentOrderIntent {
  id                String       @id @default(uuid())
  razorpay_order_id String       @unique
  invoice_id        Int
  expected_amount   Decimal
  status            String       @default("created")
  organizationId    String
  created_by        String?
  created_at        DateTime     @default(now())
  expires_at        DateTime
  verified_at       DateTime?
  invoice           invoices     @relation(fields: [invoice_id], references: [id])
  organization      Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId, invoice_id])
  @@index([status, expires_at])
  @@map("payment_order_intents")
}

model charge_catalog {
  id               Int          @id @default(autoincrement())
  category         String
  item_code        String       @unique
  item_name        String
  default_price    Decimal
  department       String?
  is_active        Boolean      @default(true)
  organizationId   String
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  created_at       DateTime @default(now())
  updated_at       DateTime @updatedAt

  organization Organization @relation(fields: [organizationId], references: [id])
  items        ItemMaster[]
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  created_at       DateTime     @default(now())
  updated_at       DateTime     @updatedAt
  hsn_sac_code     String?
  service_category String?
  tax_rate         Decimal?
  organization     Organization @relation(fields: [organizationId], references: [id])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([organizationId])
}

model insurance_providers {
  id                          Int                  @id @default(autoincrement())
  provider_name               String               @unique
  provider_code               String               @unique
  contact_email               String?
  contact_phone               String?
  address                     String?
  is_active                   Boolean              @default(true)
  organizationId              String
  created_at                  DateTime             @default(now())
  claim_submission_mode       String               @default("online")
  contact_person              String?
  covered_services            Json                 @default("[]")
  default_discount_percentage Decimal              @default(0)
  excluded_services           Json                 @default("[]")
  payment_terms_days          Int                  @default(45)
  pre_auth_required           Boolean              @default(true)
  tpa_type                    String?
  policies                    insurance_policies[]
  organization                Organization         @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model insurance_policies {
  id              Int                 @id @default(autoincrement())
  patient_id      String
  provider_id     Int
  policy_number   String              @unique
  policy_holder   String?
  plan_name       String?
  coverage_limit  Decimal?
  remaining_limit Decimal?
  valid_from      DateTime?
  valid_until     DateTime?
  status          String              @default("Active")
  organizationId  String
  created_at      DateTime            @default(now())
  corporate_name  String?
  insurer_id      String?
  member_id       String?
  payer_id        String?
  policy_type     String?
  claims          insurance_claims[]
  organization    Organization        @relation(fields: [organizationId], references: [id])
  patient         OPD_REG             @relation(fields: [patient_id], references: [patient_id])
  provider        insurance_providers @relation(fields: [provider_id], references: [id])

  @@index([organizationId])
}

model insurance_claims {
  id                    Int                @id @default(autoincrement())
  claim_number          String             @unique
  policy_id             Int
  invoice_id            Int
  admission_id          String?
  claimed_amount        Decimal
  approved_amount       Decimal?
  rejected_amount       Decimal?
  status                String             @default("Submitted")
  rejection_reason      String?
  organizationId        String
  submitted_at          DateTime           @default(now())
  reviewed_at           DateTime?
  settled_at            DateTime?
  action_status         String?
  settled_amount        Decimal?
  tds_amount            Decimal?
  zealthix_attachments  Json?
  zealthix_claim_number String?
  zealthix_remarks      String?
  admission             admissions?        @relation(fields: [admission_id], references: [admission_id])
  invoice               invoices           @relation(fields: [invoice_id], references: [id])
  organization          Organization       @relation(fields: [organizationId], references: [id])
  policy                insurance_policies @relation(fields: [policy_id], references: [id])

  @@index([organizationId])
}

model ClinicalEncounter {
  id                 String       @id @default(uuid())
  organizationId     String
  patient_id         String
  appointment_id     String?
  doctor_id          String
  encounter_date     DateTime     @default(now())
  subjective         Json         @default("{}")
  objective          Json         @default("{}")
  assessment         Json         @default("[]")
  plan               Json         @default("{}")
  allergies_reviewed Boolean      @default(false)
  status             String       @default("in_progress")
  signed_at          DateTime?
  signed_by          String?
  created_at         DateTime     @default(now())
  updated_at         DateTime     @updatedAt
  approval_status    String?      @default("draft")
  approved_at        DateTime?
  entered_by         String?
  rejected_at        DateTime?
  rejection_reason   String?
  typed_by           String?
  organization       Organization @relation(fields: [organizationId], references: [id])
  patient            OPD_REG      @relation(fields: [patient_id], references: [patient_id])

  @@index([organizationId])
  @@index([patient_id])
  @@index([doctor_id])
  @@index([approval_status])
  @@map("clinical_encounters")
}

model PatientAllergy {
  id             String       @id @default(uuid())
  organizationId String
  patient_id     String
  allergen_name  String
  allergen_type  String       @default("drug")
  reaction       String?
  severity       String       @default("moderate")
  status         String       @default("active")
  reported_date  DateTime     @default(now())
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])
  patient        OPD_REG      @relation(fields: [patient_id], references: [patient_id])

  @@index([organizationId])
  @@index([patient_id])
  @@index([allergen_type])
  @@map("patient_allergies")
}

model PaymentSplit {
  id                String       @id @default(uuid())
  organizationId    String
  invoice_id        Int
  payer_type        String
  payer_id          String?
  amount            Decimal
  payment_method    String?
  payment_reference String?
  payment_date      DateTime     @default(now())
  received_by       String?
  status            String       @default("received")
  created_at        DateTime     @default(now())
  invoice           invoices     @relation(fields: [invoice_id], references: [id])
  organization      Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([invoice_id])
  @@map("payment_splits")
}

model CorporateMaster {
  id                  String       @id @default(uuid())
  organizationId      String
  company_name        String
  company_code        String
  contact_person      String?
  contact_phone       String?
  contact_email       String?
  credit_limit        Decimal      @default(0)
  current_balance     Decimal      @default(0)
  discount_percentage Decimal      @default(0)
  payment_terms_days  Int          @default(30)
  contract_start      DateTime?
  contract_end        DateTime?
  is_active           Boolean      @default(true)
  covered_services    Json         @default("[]")
  created_at          DateTime     @default(now())
  updated_at          DateTime     @updatedAt
  patients            OPD_REG[]
  organization        Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, company_code])
  @@index([organizationId])
  @@map("corporate_masters")
}

model system_audit_logs {
  id             Int           @id @default(autoincrement())
  user_id        String?
  username       String?
  role           String?
  action         String
  module         String
  entity_type    String?
  entity_id      String?
  details        String?
  ip_address     String?
  organizationId String?
  created_at     DateTime      @default(now())
  organization   Organization? @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([action])
  @@index([module])
  @@index([created_at])
}

model triage_results {
  id                     Int          @id @default(autoincrement())
  patient_id             String?
  patient_name           String?
  symptoms               String
  duration               String?
  severity               String?
  past_medical_history   String?
  current_medications    String?
  allergies              String?
  triage_level           String
  recommended_department String?
  possible_conditions    String?
  recommended_tests      String?
  risk_alerts            String?
  clinical_summary       String?
  organizationId         String
  created_at             DateTime     @default(now())
  organization           Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model user_mfa {
  user_id        String        @id
  secret         String
  enabled        Boolean       @default(false)
  backup_codes   String?
  created_at     DateTime      @default(now())
  updated_at     DateTime      @updatedAt
  organizationId String?
  organization   Organization? @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("user_mfa")
}

model PatientPasswordSetupToken {
  id             String       @id @default(uuid())
  token_hash     String       @unique
  patient_id     String
  organizationId String
  expires_at     DateTime
  used_at        DateTime?
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])
  patient        OPD_REG      @relation(fields: [patient_id], references: [patient_id])

  @@index([patient_id, organizationId])
  @@index([expires_at])
  @@map("patient_password_setup_tokens")
}

model vital_signs {
  id               Int          @id @default(autoincrement())
  patient_id       String
  appointment_id   String?
  blood_pressure   String?
  heart_rate       Int?
  temperature      Float?
  oxygen_sat       Int?
  respiratory_rate Int?
  weight           Float?
  height           Float?
  recorded_by      String?
  organizationId   String
  created_at       DateTime     @default(now())
  blood_sugar      Float?
  encounter_id     String?
  pain_scale       Int?
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([patient_id])
}

model AiHealthAssessment {
  id                      String       @id @default(uuid())
  organizationId          String
  patient_id              String?
  session_token           String       @unique
  responses               String?
  ai_summary              String?
  risk_level              String?
  recommended_tests       String?
  recommended_specialties String?
  status                  String       @default("in_progress")
  created_at              DateTime     @default(now())
  organization            Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("ai_health_assessments")
}

model AppointmentSlot {
  id             String       @id @default(uuid())
  organizationId String
  doctor_id      String
  date           DateTime
  start_time     String
  end_time       String
  slot_type      String       @default("walkin")
  is_available   Boolean      @default(true)
  is_booked      Boolean      @default(false)
  booked_by      String?
  created_at     DateTime     @default(now())
  is_free        Boolean      @default(false)
  doctor         User         @relation(fields: [doctor_id], references: [id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([doctor_id, date])
  @@map("appointment_slots")
}

model Department {
  id                    String       @id @default(uuid())
  name                  String
  slug                  String
  head_doctor_id        String?
  is_active             Boolean      @default(true)
  organizationId        String
  created_at            DateTime     @default(now())
  base_consultation_fee Float        @default(500)
  organization          Organization @relation(fields: [organizationId], references: [id])
  wards                 wards[]

  @@unique([slug, organizationId])
  @@index([organizationId])
  @@map("departments")
}

model PrescriptionTemplate {
  id             String       @id @default(uuid())
  doctor_id      String
  name           String
  type           String       @default("prescription")
  content        String
  organizationId String
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([doctor_id])
  @@map("prescription_templates")
}

model FollowUp {
  id             String       @id @default(uuid())
  patient_id     String
  doctor_id      String
  scheduled_date DateTime
  status         String       @default("Pending")
  notes          String?
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([doctor_id])
  @@index([scheduled_date])
  @@map("follow_ups")
}

model LabSampleTracking {
  id             Int          @id @default(autoincrement())
  barcode        String       @unique
  status         String       @default("Collected")
  collected_at   DateTime?
  received_at    DateTime?
  processed_at   DateTime?
  completed_at   DateTime?
  notes          String?
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([status])
  @@map("lab_sample_tracking")
}

model LabReagentInventory {
  id             Int          @id @default(autoincrement())
  reagent_name   String
  current_stock  Int          @default(0)
  unit           String       @default("units")
  min_threshold  Int          @default(10)
  expiry_date    DateTime?
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("lab_reagent_inventory")
}

model PharmacySupplier {
  id              Int                @id @default(autoincrement())
  name            String
  contact_person  String?
  phone           String?
  email           String?
  gst_no          String?
  is_active       Boolean            @default(true)
  organizationId  String
  created_at      DateTime           @default(now())
  grns            GoodsReceiptNote[]
  organization    Organization       @relation(fields: [organizationId], references: [id])
  purchase_orders PurchaseOrder[]

  @@index([organizationId])
  @@map("pharmacy_suppliers")
}

model PurchaseOrder {
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id             Int       @id @default(autoincrement())
  po_number      String    @unique
  supplier_id    Int?
  vendor_id      Int?      // Unified vendor reference (replaces supplier_id over time)
  status         String    @default("Draft") // Draft, Submitted, Approved, Rejected, Partially Received, Received, Cancelled
  total_amount   Float     @default(0)
  gst_amount     Float     @default(0)
  ordered_at     DateTime?
  approved_at    DateTime?
  approved_by    String?
  received_at    DateTime?
  notes          String?
  organizationId String
  created_at     DateTime  @default(now())
  receiving_store_id Int?
  pr_id          Int?

  organization         Organization              @relation(fields: [organizationId], references: [id])
  supplier             PharmacySupplier?         @relation(fields: [supplier_id], references: [id])
  vendor               Vendor?                   @relation("VendorPurchaseOrders", fields: [vendor_id], references: [id])
  receiving_store      Store?                    @relation("StorePurchaseOrders", fields: [receiving_store_id], references: [id])
  pr                   PurchaseRequisition?      @relation(fields: [pr_id], references: [id])
  items                PurchaseOrderItem[]
  grns                 GoodsReceiptNote[]
  purchase_invoices    PharmacyPurchaseInvoice[]
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id                Int                       @id @default(autoincrement())
  po_number         String                    @unique
  supplier_id       Int
  status            String                    @default("Draft")
  total_amount      Float                     @default(0)
  ordered_at        DateTime?
  received_at       DateTime?
  organizationId    String
  created_at        DateTime                  @default(now())
  approved_at       DateTime?
  approved_by       String?
  gst_amount        Float                     @default(0)
  notes             String?
  vendor_id         Int?
  prId              Int?
  receivingStoreId  Int?
  grns              GoodsReceiptNote[]
  purchase_invoices PharmacyPurchaseInvoice[]
  items             PurchaseOrderItem[]
  organization      Organization              @relation(fields: [organizationId], references: [id])
  stores            stores?                   @relation(fields: [receivingStoreId], references: [id])
  supplier          PharmacySupplier          @relation(fields: [supplier_id], references: [id])
  vendor            Vendor?                   @relation("VendorPurchaseOrders", fields: [vendor_id], references: [id])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([organizationId])
  @@index([vendor_id])
  @@index([receiving_store_id])
  @@map("purchase_orders")
}

model PurchaseOrderItem {
  id                Int           @id @default(autoincrement())
  po_id             Int
  medicine_id       Int?
  item_id           Int?
  quantity_ordered  Int
  quantity_received Int           @default(0)
  unit_price        Float
  gst_rate          Float         @default(0)
  hsn_code          String?
  notes             String?
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  uom               String?
  conversion_to_base Float? @default(1.0)

  purchase_order PurchaseOrder @relation(fields: [po_id], references: [id])
  item           ItemMaster?   @relation(fields: [item_id], references: [id])
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  conversionToBase  Decimal?      @default(1)
  itemId            Int?
  uom               String?
  item_master       item_master?  @relation(fields: [itemId], references: [id])
  purchase_order    PurchaseOrder @relation(fields: [po_id], references: [id])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([po_id])
  @@index([item_id])
  @@map("purchase_order_items")
}

model GoodsReceiptNote {
  id                Int                        @id @default(autoincrement())
  grn_number        String                     @unique
  po_id             Int?
  supplier_id       Int?
  received_by       String?
  remarks           String?
  total_amount      Float                      @default(0)
  organizationId    String
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  received_at       DateTime @default(now())
  created_at        DateTime @default(now())
  store_id          Int?

  organization   Organization               @relation(fields: [organizationId], references: [id])
  purchase_order PurchaseOrder?             @relation(fields: [po_id], references: [id])
  supplier       PharmacySupplier?          @relation(fields: [supplier_id], references: [id])
  vendor         Vendor?                    @relation("VendorGRNs", fields: [vendor_id], references: [id])
  store          Store?                     @relation("StoreGRNs", fields: [store_id], references: [id])
  batches        pharmacy_batch_inventory[]
  items          GoodsReceiptNoteItem[]
  general_batches ItemBatch[]
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  received_at       DateTime                   @default(now())
  created_at        DateTime                   @default(now())
  rejected_quantity Int                        @default(0)
  rejection_reason  String?
  vendor_id         Int?
  storeId           Int?
  organization      Organization               @relation(fields: [organizationId], references: [id])
  purchase_order    PurchaseOrder?             @relation(fields: [po_id], references: [id])
  stores            stores?                    @relation(fields: [storeId], references: [id])
  supplier          PharmacySupplier?          @relation(fields: [supplier_id], references: [id])
  vendor            Vendor?                    @relation("VendorGRNs", fields: [vendor_id], references: [id])
  batches           pharmacy_batch_inventory[]
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([organizationId])
  @@index([po_id])
  @@index([vendor_id])
  @@index([store_id])
  @@map("goods_receipt_notes")
}

model PharmacyReturn {
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id               Int      @id @default(autoincrement())
  return_type      String   // patient_return, supplier_return, expired_stock, damage_writeoff
  medicine_id      Int?
  item_id          Int?
  store_id         Int?
  batch_id         String?
  batch_record_id  Int?     // FK to pharmacy_batch_inventory
  quantity         Int
  unit_cost        Float?   // batch actual cost at time of return
  reason           String?
  vendor_id        Int?     // for supplier returns
  po_id            Int?     // original PO reference
  invoice_id       Int?     // original purchase invoice reference
  original_invoice_id Int?  // patient's original sale invoice
  status           String   @default("Pending") // Pending, Approved, Processed, Rejected
  approved_by      String?
  processed_by     String?
  gl_posted        Boolean  @default(false)
  organizationId   String
  created_at       DateTime @default(now())

  organization Organization @relation(fields: [organizationId], references: [id])
  item         ItemMaster?  @relation(fields: [item_id], references: [id])
  store        Store?       @relation(fields: [store_id], references: [id])
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id                  Int          @id @default(autoincrement())
  return_type         String
  medicine_id         Int
  batch_id            String?
  quantity            Int
  reason              String?
  processed_by        String?
  organizationId      String
  created_at          DateTime     @default(now())
  approved_by         String?
  batch_record_id     Int?
  gl_posted           Boolean      @default(false)
  invoice_id          Int?
  original_invoice_id Int?
  po_id               Int?
  status              String       @default("Pending")
  unit_cost           Float?
  vendor_id           Int?
  organization        Organization @relation(fields: [organizationId], references: [id])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([organizationId])
  @@index([return_type])
  @@index([vendor_id])
  @@index([item_id])
  @@index([store_id])
  @@map("pharmacy_returns")
}

model WardStock {
  id             Int                      @id @default(autoincrement())
  ward_id        Int
  medicine_id    Int
  par_level      Int                      @default(0)
  current_stock  Int                      @default(0)
  reorder_point  Int                      @default(0)
  organizationId String
  medicine       pharmacy_medicine_master @relation(fields: [medicine_id], references: [id])
  organization   Organization             @relation(fields: [organizationId], references: [id])
  ward           wards                    @relation(fields: [ward_id], references: [ward_id])
  transactions   WardStockTransaction[]

  @@unique([ward_id, medicine_id])
  @@index([organizationId])
  @@map("ward_stock")
}

model WardStockTransaction {
  id               Int          @id @default(autoincrement())
  ward_stock_id    Int
  transaction_type String
  quantity         Int
  batch_no         String?
  performed_by     String?
  notes            String?
  organizationId   String
  created_at       DateTime     @default(now())
  organization     Organization @relation(fields: [organizationId], references: [id])
  ward_stock       WardStock    @relation(fields: [ward_stock_id], references: [id])

  @@index([ward_stock_id])
  @@index([organizationId])
  @@map("ward_stock_transactions")
}

model BedTransfer {
  id             Int          @id @default(autoincrement())
  admission_id   String
  from_bed_id    String?
  to_bed_id      String
  reason         String?
  transferred_by String?
  organizationId String
  created_at     DateTime     @default(now())
  admission      admissions   @relation(fields: [admission_id], references: [admission_id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("bed_transfers")
}

model DietPlan {
  id                     Int          @id @default(autoincrement())
  admission_id           String
  diet_type              String
  instructions           String?
  is_active              Boolean      @default(true)
  created_by             String?
  organizationId         String
  created_at             DateTime     @default(now())
  allergens              Json?
  calorie_target         Int?
  feeding_route          String?
  fluid_restriction_ml   Int?
  meal_schedule          Json?
  protein_target         Int?
  religious_restrictions String?
  review_date            DateTime?
  reviewed_by_dietitian  Boolean      @default(false)
  texture_modification   String?
  admission              admissions   @relation(fields: [admission_id], references: [admission_id])
  organization           Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("diet_plans")
}

model WardRound {
  id                   Int          @id @default(autoincrement())
  admission_id         String
  doctor_id            String
  observations         String?
  plan_changes         String?
  organizationId       String
  created_at           DateTime     @default(now())
  charge_posted        Boolean      @default(false)
  visit_fee            Decimal      @default(0)
  assessment           String?
  escalation_required  Boolean      @default(false)
  icd_codes            Json?
  next_review_in_hours Int?
  objective            String?
  orders_placed        Json?
  plan                 String?
  round_type           String       @default("Attending")
  subjective           String?
  admission            admissions   @relation(fields: [admission_id], references: [admission_id])
  organization         Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("ward_rounds")
}

model NursingTask {
  id             Int          @id @default(autoincrement())
  admission_id   String
  task_type      String
  description    String
  scheduled_at   DateTime
  completed_at   DateTime?
  assigned_to    String?
  status         String       @default("Pending")
  organizationId String
  created_at     DateTime     @default(now())
  admission      admissions   @relation(fields: [admission_id], references: [admission_id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([status])
  @@index([scheduled_at])
  @@map("nursing_tasks")
}

model PatientFeedback {
  id             Int          @id @default(autoincrement())
  patient_id     String
  appointment_id String?
  rating         Int
  comments       String?
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])
  patient        OPD_REG      @relation(fields: [patient_id], references: [patient_id])

  @@index([organizationId])
  @@map("patient_feedbacks")
}

model CashClosure {
  id             Int          @id @default(autoincrement())
  closure_date   DateTime     @default(now())
  cash_total     Float        @default(0.0)
  card_total     Float        @default(0.0)
  online_total   Float        @default(0.0)
  notes          String?
  closed_by      String
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("cash_closures")
}

model Refund {
  id             Int          @id @default(autoincrement())
  payment_id     String?
  invoice_id     String
  amount         Float
  reason         String
  status         String       @default("Pending")
  processed_by   String?
  organizationId String
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("refunds")
}

model NursingNote {
  id             Int          @id @default(autoincrement())
  admission_id   String
  nurse_id       String
  note_type      String
  details        String
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
  @@index([nurse_id])
  @@map("nursing_notes")
}

model MedicationAdministration {
  id                Int          @id @default(autoincrement())
  admission_id      String
  medication_name   String
  dose              String
  route             String
  scheduled_time    DateTime
  administered_at   DateTime?
  status            String       @default("Scheduled")
  administered_by   String?
  notes             String?
  organizationId    String
  created_at        DateTime     @default(now())
  frequency         String?
  is_prn            Boolean      @default(false)
  pain_score_after  Int?
  pain_score_before Int?
  prn_reason        String?
  organization      Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
  @@index([scheduled_time])
  @@index([status])
  @@map("medication_administrations")
}

model ShiftHandover {
  id              Int          @id @default(autoincrement())
  ward_id         Int
  from_nurse_id   String
  to_nurse_id     String
  shift_date      DateTime
  summary         String
  organizationId  String
  created_at      DateTime     @default(now())
  acknowledged_at DateTime?
  acknowledged_by String?
  critical_alerts Json?
  patients        Json?
  shift_type      String?
  organization    Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([ward_id])
  @@map("shift_handovers")
}

model Employee {
  id              Int      @id @default(autoincrement())
  user_id         String?  @unique
  employee_code   String   @unique
  name            String
  designation     String
  department_id   String?
  date_of_joining DateTime
  salary_basic    Float    @default(0)
  is_active       Boolean  @default(true)
  phone           String?
  email           String?

  // FR-HR-01
  employment_type      String?
  branch_id            String?
  grade_band           String?
  reporting_manager_id Int?
  date_of_confirmation DateTime?
  date_of_exit         DateTime?
  exit_reason          String?
  work_location        String?
  blood_group          String?
  emergency_contact    String?
  photo_url            String?

  pan_number     String?
  aadhaar_masked String?
  uan_number     String?
  pf_number      String?
  esic_number    String?

  bank_account_enc String?
  bank_ifsc_enc    String?
  bank_name_enc    String?

  payment_mode String?

  organizationId String
  created_at     DateTime @default(now())
  updated_at     DateTime @updatedAt

  organization      Organization      @relation(fields: [organizationId], references: [id])
  user                       User?                      @relation(fields: [user_id], references: [id])
  attendances                Attendance[]
  leave_requests             LeaveRequest[]
  shift_assignments          ShiftAssignment[]
  attendance_punches         AttendancePunch[]
  attendance_regularizations AttendanceRegularization[]

  documents EmployeeDocument[]

  @@index([organizationId])
  @@map("employees")
}

model EmployeeDocument {
  id String @id @default(uuid())

  employeeId Int

  documentType     String
  documentNumber   String?
  issuingAuthority String?

  validFrom DateTime?
  validTo   DateTime?

  fileUrl String?

  employee Employee @relation(fields: [employeeId], references: [id])

  @@index([employeeId])
}

model Attendance {
  id                  Int                       @id @default(autoincrement())
  employee_id         Int
  date                DateTime
  check_in            DateTime?
  check_out           DateTime?
  total_hours         Float?
  status              String                    @default("Present")
  organizationId      String
  created_at          DateTime                  @default(now())
  employee            Employee                  @relation(fields: [employee_id], references: [id])
  organization        Organization              @relation(fields: [organizationId], references: [id])
  late_minutes        Int?
  early_out_minutes   Int?
  overtime_minutes    Int?
  ot_approved         Boolean                   @default(false)
  is_night_shift      Boolean                   @default(false)
  source              AttendanceSource          @default(MANUAL)
  regularization_id   Int?
  shift_assignment_id Int?
  regularization      AttendanceRegularization? @relation(fields: [regularization_id], references: [id])

  @@index([organizationId])
  @@index([employee_id])
  @@index([date])
  @@map("attendances")
}

enum AttendanceSource {
  BIOMETRIC
  MOBILE
  MANUAL
  SYSTEM
}

model AttendancePunch {
  id              Int              @id @default(autoincrement())
  employee_id     Int
  punch_time      DateTime
  direction       String?
  source          AttendanceSource
  device_id       String?
  geo_lat         Float?
  geo_lng         Float?
  raw_payload     Json?
  organizationId  String
  created_at      DateTime         @default(now())
  employee        Employee         @relation(fields: [employee_id], references: [id])
  organization    Organization     @relation(fields: [organizationId], references: [id])

  @@unique([employee_id, punch_time, source])
  @@index([organizationId])
  @@map("attendance_punches")
}

model AttendanceDevice {
  id              Int          @id @default(autoincrement())
  device_code     String       @unique
  branch_id       String?
  vendor          String?
  webhook_secret  String
  last_seen_at    DateTime?
  is_active       Boolean      @default(true)
  organizationId  String
  created_at      DateTime     @default(now())
  organization    Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("attendance_devices")
}

model AttendanceRegularization {
  id                  Int                  @id @default(autoincrement())
  employee_id         Int
  date                DateTime
  type                RegularizationType
  reason              String
  requested_check_in  DateTime?
  requested_check_out DateTime?
  status              RegularizationStatus @default(PENDING)
  approved_by         Int?
  approved_at         DateTime?
  rejection_reason    String?
  organizationId      String
  created_at          DateTime             @default(now())
  employee            Employee             @relation(fields: [employee_id], references: [id])
  organization        Organization         @relation(fields: [organizationId], references: [id])
  attendance          Attendance[]

  @@index([organizationId])
  @@map("attendance_regularizations")
}

enum RegularizationType {
  MISSED_PUNCH
  ON_DUTY
  WFH
}

enum RegularizationStatus {
  PENDING
  APPROVED
  REJECTED
}

model AttendancePolicy {
  id                        Int          @id @default(autoincrement())
  organizationId            String       @unique
  grace_minutes             Int          @default(10)
  late_marks_for_half_day   Int          @default(3)
  min_hours_full_day        Float        @default(8.0)
  min_hours_half_day        Float        @default(4.0)
  sandwich_rule_enabled     Boolean      @default(false)
  max_regularizations_month Int          @default(3)
  created_at                DateTime     @default(now())
  updated_at                DateTime     @updatedAt
  organization              Organization @relation(fields: [organizationId], references: [id])

  @@map("attendance_policies")
}

model LeaveType {
  id             Int            @id @default(autoincrement())
  name           String
  days_per_year  Int            @default(12)
  carry_forward  Boolean        @default(false)
  organizationId String
  created_at     DateTime       @default(now())
  leave_requests LeaveRequest[]
  organization   Organization   @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("leave_types")
}

model LeaveRequest {
  id             Int          @id @default(autoincrement())
  employee_id    Int
  leave_type_id  Int
  from_date      DateTime
  to_date        DateTime
  reason         String?
  status         String       @default("Pending")
  approved_by    String?
  organizationId String
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  employee       Employee     @relation(fields: [employee_id], references: [id])
  leave_type     LeaveType    @relation(fields: [leave_type_id], references: [id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([employee_id])
  @@index([status])
  @@map("leave_requests")
}

model ShiftPattern {
  id             Int               @id @default(autoincrement())
  name           String
  start_time     String
  end_time       String
  is_overnight   Boolean           @default(false)
  organizationId String
  created_at     DateTime          @default(now())
  assignments    ShiftAssignment[]
  organization   Organization      @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("shift_patterns")
}

model ShiftAssignment {
  id               Int          @id @default(autoincrement())
  employee_id      Int
  shift_pattern_id Int
  date             DateTime
  status           String       @default("ASSIGNED") // ASSIGNED, SWAP_PENDING, SWAPPED, CANCELLED
  notes            String?
  organizationId   String
  created_at       DateTime     @default(now())
  employee         Employee     @relation(fields: [employee_id], references: [id])
  organization     Organization @relation(fields: [organizationId], references: [id])
  shift_pattern    ShiftPattern @relation(fields: [shift_pattern_id], references: [id])

  @@unique([employee_id, date, organizationId])
  @@index([organizationId])
  @@index([employee_id])
  @@index([date])
  @@map("shift_assignments")
}

model ShiftSwapRequest {
  id                      Int          @id @default(autoincrement())
  requester_assignment_id Int
  target_assignment_id    Int
  requester_employee_id   Int
  target_employee_id      Int
  status                  String       @default("PENDING") // PENDING, ACCEPTED, ADMIN_APPROVED, REJECTED, CANCELLED
  requester_reason        String?
  admin_notes             String?
  responded_at            DateTime?
  approved_at             DateTime?
  approved_by             String?
  organizationId          String
  created_at              DateTime     @default(now())
  updated_at              DateTime     @updatedAt

  organization Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([status])
  @@map("shift_swap_requests")
}

model Notification {
  id             Int          @id @default(autoincrement())
  user_id        String
  title          String
  body           String
  type           String       @default("info")
  is_read        Boolean      @default(false)
  link           String?
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([user_id])
  @@index([is_read])
  @@map("notifications")
}

model ExpenseCategory {
  id             Int               @id @default(autoincrement())
  name           String
  code           String
  parent_id      Int?
  is_active      Boolean           @default(true)
  organizationId String
  created_at     DateTime          @default(now())
  budget_lines   BudgetLine[]
  organization   Organization      @relation(fields: [organizationId], references: [id])
  parent         ExpenseCategory?  @relation("CategoryHierarchy", fields: [parent_id], references: [id])
  children       ExpenseCategory[] @relation("CategoryHierarchy")
  expenses       Expense[]

  @@unique([code, organizationId])
  @@index([organizationId])
  @@map("expense_categories")
}

model Vendor {
  id                         Int                       @id @default(autoincrement())
  vendor_name                String
  vendor_code                String
  contact_person             String?
  phone                      String?
  email                      String?
  gst_number                 String?
  pan_number                 String?
  bank_name                  String?
  bank_account               String?
  bank_ifsc                  String?
  address                    String?
  is_active                  Boolean                   @default(true)
  organizationId             String
  created_at                 DateTime                  @default(now())
  drug_license_expiry        DateTime?
  drug_license_number        String?
  is_pharmacy_supplier       Boolean                   @default(false)
  pharmacy_payment_terms     Int?                      @default(30)
  asset_maintenance          AssetMaintenance[]
  expenses                   Expense[]
  fixed_assets               FixedAsset[]
  pharmacy_grns              GoodsReceiptNote[]        @relation("VendorGRNs")
  item_batches               item_batches[]
  item_vendors               item_vendors[]
  pharmacy_purchase_invoices PharmacyPurchaseInvoice[]
<<<<<<< HEAD
  item_vendors             ItemVendor[]
  item_batches             ItemBatch[]

=======
<<<<<<< HEAD
  item_vendors             ItemVendor[]
  item_batches             ItemBatch[]
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  pharmacy_purchase_orders   PurchaseOrder[]           @relation("VendorPurchaseOrders")
  organization               Organization              @relation(fields: [organizationId], references: [id])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@unique([vendor_code, organizationId])
  @@index([organizationId])
  @@map("vendors")
}

model Expense {
  id             Int             @id @default(autoincrement())
  expense_number String          @unique
  category_id    Int
  vendor_id      Int?
  description    String
  amount         Decimal
  tax_amount     Decimal         @default(0)
  total_amount   Decimal
  payment_method String?
  payment_date   DateTime?
  reference_no   String?
  status         String          @default("Pending")
  approved_by    String?
  notes          String?
  organizationId String
  created_at     DateTime        @default(now())
  updated_at     DateTime        @updatedAt
  cgst_amount    Decimal?        @db.Decimal(15, 2)
  gst_rate       Decimal?        @db.Decimal(5, 2)
  hsn_sac_code   String?
  igst_amount    Decimal?        @db.Decimal(15, 2)
  is_inter_state Boolean         @default(false)
  sgst_amount    Decimal?        @db.Decimal(15, 2)
  vendor_gstin   String?
  receipt_key    String?
  category       ExpenseCategory @relation(fields: [category_id], references: [id])
  organization   Organization    @relation(fields: [organizationId], references: [id])
  vendor         Vendor?         @relation(fields: [vendor_id], references: [id])

  @@index([organizationId])
  @@index([status])
  @@index([category_id])
  @@index([created_at])
  @@map("expenses")
}

model TaxConfig {
  id             Int          @id @default(autoincrement())
  tax_name       String
  tax_code       String
  rate           Decimal
  is_default     Boolean      @default(false)
  applicable_to  String?
  is_active      Boolean      @default(true)
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@unique([tax_code, organizationId])
  @@index([organizationId])
  @@map("tax_configs")
}

model PatientConsent {
  id             Int       @id @default(autoincrement())
  patient_id     String
  consent_type   String
  granted        Boolean   @default(false)
  granted_at     DateTime?
  revoked_at     DateTime?
  ip_address     String?
  organizationId String
  created_at     DateTime  @default(now())
  updated_at     DateTime  @updatedAt

  @@unique([patient_id, consent_type, organizationId])
  @@index([patient_id])
  @@index([organizationId])
  @@map("patient_consents")
}

model PasswordResetOTP {
  id             Int      @id @default(autoincrement())
  patient_id     String
  otp_hash       String
  phone          String
  expires_at     DateTime
  is_used        Boolean  @default(false)
  attempts       Int      @default(0)
  organizationId String
  created_at     DateTime @default(now())

  @@index([patient_id])
  @@index([organizationId])
  @@map("password_reset_otps")
}

model PatientDeposit {
  id                 Int          @id @default(autoincrement())
  deposit_number     String       @unique
  patient_id         String
  admission_id       String?
  amount             Decimal
  payment_method     String
  payment_ref        String?
  status             String       @default("Active")
  applied_to_invoice Int?
  applied_amount     Decimal      @default(0)
  refunded_amount    Decimal      @default(0)
  collected_by       String?
  notes              String?
  organizationId     String
  created_at         DateTime     @default(now())
  updated_at         DateTime     @updatedAt
  organization       Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([patient_id])
  @@index([status])
  @@map("patient_deposits")
}

model CreditNote {
  id                  Int          @id @default(autoincrement())
  credit_note_number  String       @unique
  original_invoice_id Int
  reason              String
  items               String?
  total_amount        Decimal
  status              String       @default("Draft")
  approved_by         String?
  notes               String?
  organizationId      String
  created_at          DateTime     @default(now())
  updated_at          DateTime     @updatedAt
  organization        Organization @relation(fields: [organizationId], references: [id])
  original_invoice    invoices     @relation(fields: [original_invoice_id], references: [id])

  @@index([organizationId])
  @@index([original_invoice_id])
  @@map("credit_notes")
}

model FinancialPeriod {
  id              Int               @id @default(autoincrement())
  period_name     String
  period_type     String
  start_date      DateTime
  end_date        DateTime
  status          String            @default("Open")
  closed_by       String?
  total_revenue   Decimal           @default(0)
  total_expenses  Decimal           @default(0)
  net_result      Decimal           @default(0)
  organizationId  String
  created_at      DateTime          @default(now())
  organization    Organization      @relation(fields: [organizationId], references: [id])
  GL_JournalEntry GL_JournalEntry[]

  @@index([organizationId])
  @@index([status])
  @@map("financial_periods")
}

model BankTransaction {
  id                 Int          @id @default(autoincrement())
  transaction_date   DateTime
  description        String
  reference          String?
  debit              Decimal      @default(0)
  credit             Decimal      @default(0)
  balance            Decimal      @default(0)
  matched_payment_id Int?
  matched_expense_id Int?
  reconciled         Boolean      @default(false)
  bank_account       String?
  organizationId     String
  created_at         DateTime     @default(now())
  organization       Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([reconciled])
  @@map("bank_transactions")
}

model DunningRule {
  id             Int          @id @default(autoincrement())
  rule_name      String
  days_overdue   Int
  action_type    String
  template_text  String?
  is_active      Boolean      @default(true)
  organizationId String
  created_at     DateTime     @default(now())
  logs           DunningLog[]
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("dunning_rules")
}

model DunningLog {
  id             Int          @id @default(autoincrement())
  invoice_id     Int
  rule_id        Int
  patient_id     String
  action_taken   String
  status         String       @default("Sent")
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])
  rule           DunningRule  @relation(fields: [rule_id], references: [id])

  @@index([organizationId])
  @@index([invoice_id])
  @@map("dunning_logs")
}

model DataImportJob {
  id                 String       @id @default(uuid())
  import_type        String
  file_name          String
  file_size          Int
  total_rows         Int          @default(0)
  processed_rows     Int          @default(0)
  successful_rows    Int          @default(0)
  failed_rows        Int          @default(0)
  skipped_rows       Int          @default(0)
  status             String       @default("uploaded")
  column_mapping     Json?
  validation_summary Json?
  error_log          Json?
  import_data        Json?
  started_at         DateTime?
  completed_at       DateTime?
  created_by         String
  organizationId     String
  created_at         DateTime     @default(now())
  updated_at         DateTime     @updatedAt
  organization       Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([status])
  @@index([created_at])
  @@map("data_import_jobs")
}

model ArchivedPatientRecord {
  id                  String       @id @default(uuid())
  original_patient_id String
  patient_data        Json
  clinical_data       Json?
  financial_data      Json?
  archive_reason      String       @default("retention_policy")
  original_created_at DateTime
  archived_at         DateTime     @default(now())
  organizationId      String
  organization        Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([original_patient_id])
  @@index([archived_at])
  @@map("archived_patient_records")
}

model ZealthixApiKey {
  id             String       @id @default(uuid())
  api_key_hash   String       @unique
  label          String?
  is_active      Boolean      @default(true)
  last_used_at   DateTime?
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@map("zealthix_api_keys")
}

model PillReminder {
  id              String       @id @default(uuid())
  patient_id      String
  doctor_id       String
  medication_name String
  dosage          String
  schedule_times  String[]
  start_date      DateTime
  end_date        DateTime
  notes           String?
  status          String       @default("Active")
  organizationId  String
  created_at      DateTime     @default(now())
  updated_at      DateTime     @updatedAt
  organization    Organization @relation(fields: [organizationId], references: [id])
  patient         OPD_REG      @relation(fields: [patient_id], references: [patient_id])

  @@index([organizationId])
  @@index([patient_id])
  @@map("pill_reminders")
}

model MessageDeliveryLog {
  id             String       @id @default(uuid())
  organizationId String
  message_id     String
  patient_phone  String?
  patient_email  String?
  channel        String
  event_type     String
  status         String
  error_detail   String?
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId, patient_phone])
  @@index([message_id])
  @@map("message_delivery_log")
}

model WhatsAppIncomingMessage {
  id             String       @id @default(uuid())
  organizationId String
  from_phone     String
  message_type   String
  message_body   String?
  wa_message_id  String       @unique
  timestamp      DateTime
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId, from_phone])
  @@map("whatsapp_incoming_messages")
}

model IpdServiceMaster {
  id               Int             @id @default(autoincrement())
  service_code     String
  service_name     String
  service_category String
  default_rate     Decimal
  hsn_sac_code     String?
  tax_rate         Decimal         @default(0)
  is_active        Boolean         @default(true)
  organizationId   String
  created_at       DateTime        @default(now())
  updated_at       DateTime        @updatedAt
  organization     Organization    @relation(fields: [organizationId], references: [id])
  tariff_rates     IpdTariffRate[]

  @@unique([service_code, organizationId])
  @@index([organizationId])
  @@index([service_category])
  @@map("ipd_service_master")
}

model IpdTariffRate {
  id                Int              @id @default(autoincrement())
  service_id        Int
  tariff_category   String           @default("general")
  rate              Decimal          @default(0)
  is_active         Boolean          @default(true)
  organizationId    String
  created_at        DateTime         @default(now())
  deluxe_rate       Decimal          @default(0)
  general_rate      Decimal          @default(0)
  icu_rate          Decimal          @default(0)
  private_rate      Decimal          @default(0)
  semi_private_rate Decimal          @default(0)
  organization      Organization     @relation(fields: [organizationId], references: [id])
  service           IpdServiceMaster @relation(fields: [service_id], references: [id])

  @@unique([service_id, organizationId])
  @@index([organizationId])
  @@map("ipd_tariff_rates")
}

model IpdPackage {
  id                 Int                   @id @default(autoincrement())
  package_code       String
  package_name       String
  description        String?
  total_amount       Decimal
  validity_days      Int                   @default(7)
  inclusions         Json                  @default("[]")
  is_active          Boolean               @default(true)
  organizationId     String
  created_at         DateTime              @default(now())
  updated_at         DateTime              @updatedAt
  exclusions         Json                  @default("[]")
  admission_packages IpdAdmissionPackage[]
  organization       Organization          @relation(fields: [organizationId], references: [id])

  @@unique([package_code, organizationId])
  @@index([organizationId])
  @@map("ipd_packages")
}

model IpdAdmissionPackage {
  id             Int          @id @default(autoincrement())
  admission_id   String
  package_id     Int
  applied_amount Decimal
  is_broken_open Boolean      @default(false)
  broken_at      DateTime?
  applied_by     String?
  organizationId String
  created_at     DateTime     @default(now())
  admission      admissions   @relation(fields: [admission_id], references: [admission_id])
  organization   Organization @relation(fields: [organizationId], references: [id])
  package        IpdPackage   @relation(fields: [package_id], references: [id])

  @@index([admission_id])
  @@index([organizationId])
  @@map("ipd_admission_packages")
}

model IpdEstimate {
  id                Int          @id @default(autoincrement())
  estimate_number   String       @unique
  patient_id        String
  diagnosis         String?
  expected_los_days Int          @default(3)
  room_category     String?
  package_id        Int?
  items             Json         @default("[]")
  total_estimate    Decimal      @default(0)
  deposit_required  Decimal      @default(0)
  status            String       @default("Draft")
  valid_until       DateTime?
  notes             String?
  created_by        String?
  organizationId    String
  created_at        DateTime     @default(now())
  updated_at        DateTime     @updatedAt
  organization      Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([patient_id])
  @@map("ipd_estimates")
}

model IpdChargePosting {
  id              Int          @id @default(autoincrement())
  admission_id    String
  invoice_item_id Int?
  source_module   String
  source_ref_id   String?
  description     String
  amount          Decimal
  posted_by       String?
  posted_at       DateTime     @default(now())
  organizationId  String
  organization    Organization @relation(fields: [organizationId], references: [id])

  @@index([admission_id])
  @@index([organizationId])
  @@map("ipd_charge_postings")
}

model whatsapp_log {
  id        String   @id @default(uuid())
  messageId String?  @unique
  to        String
  type      String
  patientId String?
  status    String   @default("pending")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  patient   OPD_REG? @relation(fields: [patientId], references: [patient_id])
}

model NursingAssessmentAlert {
  id                   String       @id @default(uuid())
  admission_id         String
  patient_id           String
  arrival_in_unit_at   DateTime     @default(now())
  assessment_due_at    DateTime
  assessment_completed Boolean      @default(false)
  completed_at         DateTime?
  alert_sent_to_group  Boolean      @default(false)
  alert_sent_at        DateTime?
  doctor_group_id      String?
  organizationId       String
  created_at           DateTime     @default(now())
  organization         Organization @relation(fields: [organizationId], references: [id])

  @@index([admission_id])
  @@index([organizationId])
  @@index([assessment_completed])
  @@map("nursing_assessment_alerts")
}

model ClinicalOrder {
  id             String       @id @default(uuid())
  admission_id   String
  patient_id     String
  doctor_id      String
  order_type     String
  order_details  Json
  status         String       @default("pending")
  priority       String       @default("routine")
  ordered_at     DateTime     @default(now())
  completed_at   DateTime?
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([admission_id])
  @@index([organizationId])
  @@index([status])
  @@map("clinical_orders")
}

model PhysicianOrder {
  id              String       @id @default(uuid())
  admission_id    String
  patient_id      String
  doctor_id       String
  order_category  String
  order_text      String
  frequency       String?
  duration        String?
  status          String       @default("active")
  ordered_at      DateTime     @default(now())
  discontinued_at DateTime?
  organizationId  String
  created_at      DateTime     @default(now())
  organization    Organization @relation(fields: [organizationId], references: [id])

  @@index([admission_id])
  @@index([organizationId])
  @@index([status])
  @@map("physician_orders")
}

model ActiveMedication {
  id                     String       @id @default(uuid())
  admission_id           String
  patient_id             String
  medication_name        String
  dosage                 String
  route                  String
  frequency              String
  start_date             DateTime     @default(now())
  end_date               DateTime?
  prescribed_by          String
  status                 String       @default("active")
  discontinuation_reason String?
  organizationId         String
  created_at             DateTime     @default(now())
  organization           Organization @relation(fields: [organizationId], references: [id])

  @@index([admission_id])
  @@index([organizationId])
  @@index([status])
  @@map("active_medications")
}

model OrderSet {
  id                    String       @id @default(uuid())
  doctor_id             String
  name                  String
  description           String?
  chief_complaints      Json?        @default("[]")
  gross_examination     Json?        @default("[]")
  diagnosis             Json?        @default("[]")
  advice_investigations Json?        @default("[]")
  advice_medications    Json?        @default("[]")
  op_procedures         Json?        @default("[]")
  is_favorite           Boolean      @default(false)
  usage_count           Int          @default(0)
  organizationId        String
  created_at            DateTime     @default(now())
  updated_at            DateTime     @updatedAt
  organization          Organization @relation(fields: [organizationId], references: [id])

  @@index([doctor_id])
  @@index([organizationId])
  @@map("order_sets")
}

model DoctorInvestigationFavorite {
  id                 String       @id @default(uuid())
  doctor_id          String
  investigation_name String
  category           String?
  display_order      Int          @default(0)
  organizationId     String
  created_at         DateTime     @default(now())
  organization       Organization @relation(fields: [organizationId], references: [id])

  @@unique([doctor_id, investigation_name])
  @@index([doctor_id])
  @@index([organizationId])
  @@map("doctor_investigation_favorites")
}

model ICD10Master {
  id          String   @id @default(uuid())
  code        String   @unique
  description String
  category    String?
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now())

  @@index([code])
  @@map("icd10_master")
}

model ReferralOrder {
  id             String       @id @default(uuid())
  admission_id   String?
  patient_id     String
  referred_by    String
  referred_to    String
  department     String?
  location       String?
  reason         String
  status         String       @default("pending")
  priority       String       @default("routine")
  notes          String?
  referred_at    DateTime     @default(now())
  accepted_at    DateTime?
  completed_at   DateTime?
  organizationId String
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([patient_id])
  @@index([organizationId])
  @@index([status])
  @@map("referral_orders")
}

model patient_external_records {
  id             Int       @id @default(autoincrement())
  patient_id     String
  organizationId String
  title          String
  description    String?
  hospital_name  String?
  record_date    DateTime?
  file_url       String?
  file_name      String?
  created_at     DateTime  @default(now())
  updated_at     DateTime  @updatedAt

  @@index([patient_id])
  @@index([organizationId])
}

model IPDVitals {
  id               Int          @id @default(autoincrement())
  admission_id     String
  patient_id       String
  organizationId   String
  bp_systolic      Int?
  bp_diastolic     Int?
  heart_rate       Int?
  temperature      Float?
  respiratory_rate Int?
  spo2             Int?
  pain_score       Int?
  consciousness    String?
  blood_sugar      Int?
  urine_output_ml  Int?
  news_score       Int?
  news_level       String?
  recorded_by      String?
  created_at       DateTime     @default(now())
  admission        admissions   @relation(fields: [admission_id], references: [admission_id])
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
  @@index([created_at])
  @@map("ipd_vitals")
}

model NursingAssessment {
  id               Int          @id @default(autoincrement())
  admission_id     String
  organizationId   String
  assessment_type  String       @default("initial")
  consciousness    String?
  pain_score       Int?
  fall_risk_score  Int?
  braden_score     Int?
  nutrition_screen String?
  mobility         String?
  continence       String?
  skin_assessment  Json?
  safety_measures  Json?
  care_plan        Json?
  assessed_by      String?
  created_at       DateTime     @default(now())
  admission        admissions   @relation(fields: [admission_id], references: [admission_id])
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
  @@map("nursing_assessments")
}

model InsurancePreAuth {
  id                  String       @id @default(uuid())
  admission_id        String?
  patient_id          String
  organizationId      String
  policy_id           String?
  tpa_name            String?
  pre_auth_number     String?
  requested_amount    Decimal      @default(0)
  approved_amount     Decimal?
  status              String       @default("Submitted")
  submission_type     String       @default("Planned")
  diagnosis_icd       String?
  procedure_codes     Json?
  documents           Json?
  queries             Json?
  enhancement_history Json?
  tpa_remarks         String?
  submitted_at        DateTime     @default(now())
  responded_at        DateTime?
  created_at          DateTime     @default(now())
  updated_at          DateTime     @updatedAt
  admission           admissions?  @relation(fields: [admission_id], references: [admission_id])
  organization        Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
  @@map("insurance_preauths")
}

model AdmissionConsultant {
  id             String       @id @default(uuid())
  admission_id   String
  organizationId String
  doctor_id      String?
  doctor_name    String
  specialty      String?
  consulted_at   DateTime     @default(now())
  notes          String?
  status         String       @default("Active")
  admission      admissions   @relation(fields: [admission_id], references: [admission_id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
  @@map("admission_consultants")
}

model GL_Account {
  id                                       String           @id @default(uuid())
  organizationId                           String
  account_code                             String
  account_name                             String
  account_type                             String
  account_group                            String?
  parent_id                                String?
  normal_balance                           String
  opening_balance                          Decimal          @default(0) @db.Decimal(15, 2)
  current_balance                          Decimal          @default(0) @db.Decimal(15, 2)
  tally_ledger_name                        String?
  tally_group                              String?
  is_active                                Boolean          @default(true)
  created_at                               DateTime         @default(now())
  updated_at                               DateTime         @updatedAt
  admissionsAdmission_id                   String?
  asset_categories_as_asset_account        AssetCategory[]  @relation("AssetAccount")
  asset_categories_as_depreciation_account AssetCategory[]  @relation("DepreciationAccount")
  asset_categories_as_expense_account      AssetCategory[]  @relation("ExpenseAccount")
  budget_lines                             BudgetLine[]
  item_categories_as_inventory             ItemCategory[]   @relation("CategoryInventoryAccount")
  item_categories_as_expense               ItemCategory[]   @relation("CategoryExpenseAccount")
  item_categories_as_cogs                  ItemCategory[]   @relation("CategoryCOGSAccount")
  admissions                               admissions?      @relation(fields: [admissionsAdmission_id], references: [admission_id])
  organization                             Organization     @relation(fields: [organizationId], references: [id])
  parent                                   GL_Account?      @relation("AccountHierarchy", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  children                                 GL_Account[]     @relation("AccountHierarchy")
  journal_lines                            GL_JournalLine[]

  @@unique([account_code, organizationId])
  @@index([organizationId])
  @@index([parent_id])
  @@index([account_type])
  @@map("gl_accounts")
}

model GL_JournalEntry {
  id                     String              @id @default(uuid())
  organizationId         String
  journal_number         String
  entry_date             DateTime
  period_id              Int?
  entry_type             String
  reference_type         String?
  reference_id           String?
  reference_number       String?
  narration              String
  total_debit            Decimal             @db.Decimal(15, 2)
  total_credit           Decimal             @db.Decimal(15, 2)
  status                 String              @default("Draft")
  reversal_entry_id      String?             @unique
  created_by             String?
  created_at             DateTime            @default(now())
  updated_at             DateTime            @updatedAt
  admissionsAdmission_id String?
  depreciation_entries   DepreciationEntry[]
  admissions             admissions?         @relation(fields: [admissionsAdmission_id], references: [admission_id])
  organization           Organization        @relation(fields: [organizationId], references: [id])
  period                 FinancialPeriod?    @relation(fields: [period_id], references: [id])
  reversal_entry         GL_JournalEntry?    @relation("JournalReversal", fields: [reversal_entry_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  reversed_by            GL_JournalEntry?    @relation("JournalReversal")
  lines                  GL_JournalLine[]
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  depreciation_entries   DepreciationEntry[]
  inventory_movements    InventoryMovement[]
  admissions             admissions?         @relation(fields: [admissionsAdmission_id], references: [admission_id])
  admissionsAdmission_id String?
<<<<<<< HEAD

=======
=======
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a

  @@unique([journal_number, organizationId])
  @@index([organizationId])
  @@index([period_id])
  @@index([entry_date])
  @@index([entry_type])
  @@index([reference_type, reference_id])
  @@map("gl_journal_entries")
}

model GL_JournalLine {
  id                     String          @id @default(uuid())
  organizationId         String
  journal_id             String
  line_number            Int
  account_id             String
  debit_amount           Decimal         @default(0) @db.Decimal(15, 2)
  credit_amount          Decimal         @default(0) @db.Decimal(15, 2)
  description            String?
  cost_center            String?
  created_at             DateTime        @default(now())
  admissionsAdmission_id String?
  bill_reference         String?
  bill_alloc_type        String?
  account                GL_Account      @relation(fields: [account_id], references: [id])
  admissions             admissions?     @relation(fields: [admissionsAdmission_id], references: [admission_id])
  journal                GL_JournalEntry @relation(fields: [journal_id], references: [id], onDelete: Cascade)
  organization           Organization    @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([journal_id])
  @@index([account_id])
  @@map("gl_journal_lines")
}

model TallyExport {
  id                     String       @id @default(uuid())
  organizationId         String
  export_number          String
  export_type            String
  export_format          String       @default("XML")
  start_date             DateTime?
  end_date               DateTime?
  file_path              String?
  file_size              BigInt?
  record_count           Int          @default(0)
  status                 String       @default("Pending")
  error_message          String?
  created_by             String?
  created_at             DateTime     @default(now())
  completed_at           DateTime?
  admissionsAdmission_id String?
  admissions             admissions?  @relation(fields: [admissionsAdmission_id], references: [admission_id])
  organization           Organization @relation(fields: [organizationId], references: [id])

  @@unique([export_number, organizationId])
  @@index([organizationId])
  @@index([status])
  @@index([created_at])
  @@map("tally_exports")
}

model TallyLedgerMapping {
  id                String    @id @default(uuid())
  organizationId    String
  gl_account_id     String
  his_ledger_name   String
  tally_ledger_name String?
  tally_group       String?
  tally_guid        String?
  sync_status       String    @default("pending")
  error_message     String?
  last_synced_at    DateTime?
  created_at        DateTime  @default(now())
  updated_at        DateTime  @updatedAt

  @@unique([organizationId, gl_account_id])
  @@index([organizationId])
  @@index([sync_status])
  @@map("tally_ledger_mapping")
}

model TallyVoucherMapping {
  id                   String    @id @default(uuid())
  organizationId       String
  gl_journal_entry_id  String
  voucher_type         String
  his_voucher_number   String
  tally_voucher_number String?
  tally_guid           String?
  sync_status          String    @default("pending")
  error_message        String?
  last_synced_at       DateTime?
  created_at           DateTime  @default(now())
  updated_at           DateTime  @updatedAt

  @@unique([organizationId, gl_journal_entry_id])
  @@index([organizationId])
  @@index([sync_status])
  @@map("tally_voucher_mapping")
}

model TallySyncLog {
  id               String   @id @default(uuid())
  organizationId   String
  entity_type      String
  entity_id        String?
  action           String
  status           String
  request_payload  String?
  response_payload String?
  error_message    String?
  records_count    Int      @default(0)
  created_by       String?
  created_at       DateTime @default(now())

  @@index([organizationId])
  @@index([entity_type])
  @@index([status])
  @@index([created_at])
  @@map("tally_sync_logs")
}

model GST_Invoice_Register {
  id                     String       @id @default(uuid())
  organizationId         String
  invoice_id             String?
  expense_id             String?
  transaction_type       String
  invoice_number         String
  invoice_date           DateTime
  gstin_supplier         String?
  gstin_recipient        String?
  recipient_name         String?
  recipient_state        String?
  place_of_supply        String?
  is_inter_state         Boolean      @default(false)
  reverse_charge         Boolean      @default(false)
  invoice_type           String       @default("Regular")
  taxable_amount         Decimal      @db.Decimal(15, 2)
  cgst_amount            Decimal      @default(0) @db.Decimal(15, 2)
  sgst_amount            Decimal      @default(0) @db.Decimal(15, 2)
  igst_amount            Decimal      @default(0) @db.Decimal(15, 2)
  cess_amount            Decimal      @default(0) @db.Decimal(15, 2)
  total_tax              Decimal      @db.Decimal(15, 2)
  total_invoice_value    Decimal      @db.Decimal(15, 2)
  hsn_sac_code           String?
  gst_rate               Decimal?     @db.Decimal(5, 2)
  itc_eligibility        String?
  itc_claimed_cgst       Decimal      @default(0) @db.Decimal(15, 2)
  itc_claimed_sgst       Decimal      @default(0) @db.Decimal(15, 2)
  itc_claimed_igst       Decimal      @default(0) @db.Decimal(15, 2)
  filed_in_gstr1         Boolean      @default(false)
  filed_in_gstr2         Boolean      @default(false)
  filing_period          String?
  created_at             DateTime     @default(now())
  admissionsAdmission_id String?
  admissions             admissions?  @relation(fields: [admissionsAdmission_id], references: [admission_id])
  organization           Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([invoice_date])
  @@index([transaction_type])
  @@index([filing_period])
  @@map("gst_invoice_register")
}

model GST_Return_Filing {
  id                       String       @id @default(uuid())
  organizationId           String
  return_type              String
  filing_period            String
  return_period_start      DateTime
  return_period_end        DateTime
  status                   String       @default("Draft")
  filed_date               DateTime?
  acknowledgment_no        String?
  total_outward_supplies   Decimal?     @db.Decimal(15, 2)
  total_taxable_value      Decimal?     @db.Decimal(15, 2)
  total_cgst               Decimal?     @db.Decimal(15, 2)
  total_sgst               Decimal?     @db.Decimal(15, 2)
  total_igst               Decimal?     @db.Decimal(15, 2)
  outward_taxable_supplies Decimal?     @db.Decimal(15, 2)
  outward_tax_liability    Decimal?     @db.Decimal(15, 2)
  itc_available            Decimal?     @db.Decimal(15, 2)
  itc_reversed             Decimal?     @db.Decimal(15, 2)
  net_tax_payable          Decimal?     @db.Decimal(15, 2)
  interest                 Decimal?     @db.Decimal(15, 2)
  late_fee                 Decimal?     @db.Decimal(15, 2)
  json_data                String?
  file_path                String?
  created_at               DateTime     @default(now())
  updated_at               DateTime     @updatedAt
  admissionsAdmission_id   String?
  admissions               admissions?  @relation(fields: [admissionsAdmission_id], references: [admission_id])
  organization             Organization @relation(fields: [organizationId], references: [id])

  @@unique([return_type, filing_period, organizationId])
  @@index([organizationId])
  @@index([filing_period])
  @@map("gst_return_filings")
}

model HSN_SAC_Master {
  id                     String       @id @default(uuid())
  organizationId         String
  code                   String
  description            String
  code_type              String
  uqc                    String?
  gst_rate               Decimal      @db.Decimal(5, 2)
  is_active              Boolean      @default(true)
  admissionsAdmission_id String?
  admissions             admissions?  @relation(fields: [admissionsAdmission_id], references: [admission_id])
  organization           Organization @relation(fields: [organizationId], references: [id])

  @@unique([code, organizationId])
  @@index([organizationId])
  @@map("hsn_sac_master")
}

model AssetCategory {
  id                         String       @id @default(uuid())
  organizationId             String
  category_name              String
  category_code              String
  asset_type                 String
  depreciation_method        String
  depreciation_rate          Decimal      @db.Decimal(5, 2)
  useful_life_years          Int?
  gl_asset_account_id        String?
  gl_depreciation_account_id String?
  gl_expense_account_id      String?
  is_active                  Boolean      @default(true)
  created_at                 DateTime     @default(now())
  updated_at                 DateTime     @updatedAt
  gl_asset_account           GL_Account?  @relation("AssetAccount", fields: [gl_asset_account_id], references: [id])
  gl_depreciation_account    GL_Account?  @relation("DepreciationAccount", fields: [gl_depreciation_account_id], references: [id])
  gl_expense_account         GL_Account?  @relation("ExpenseAccount", fields: [gl_expense_account_id], references: [id])
  organization               Organization @relation(fields: [organizationId], references: [id])
  assets                     FixedAsset[]

  @@unique([category_code, organizationId])
  @@index([organizationId])
  @@map("asset_categories")
}

model FixedAsset {
  id                       String              @id @default(uuid())
  organizationId           String
  asset_code               String
  asset_name               String
  category_id              String
  description              String?
  location                 String?
  department               String?
  acquisition_date         DateTime
  acquisition_cost         Decimal             @db.Decimal(15, 2)
  vendor_id                Int?
  invoice_number           String?
  warranty_expiry          DateTime?
  depreciation_method      String
  depreciation_rate        Decimal             @db.Decimal(5, 2)
  salvage_value            Decimal             @default(0) @db.Decimal(15, 2)
  accumulated_depreciation Decimal             @default(0) @db.Decimal(15, 2)
  book_value               Decimal             @db.Decimal(15, 2)
  last_depreciation_date   DateTime?
  status                   String              @default("Active")
  disposed_date            DateTime?
  disposal_value           Decimal?            @db.Decimal(15, 2)
  disposal_reason          String?
  maintenance_schedule     String?
  last_maintenance_date    DateTime?
  next_maintenance_date    DateTime?
  serial_number            String?
  manufacturer             String?
  model_number             String?
  is_capitalized           Boolean             @default(true)
  capitalization_date      DateTime?
  created_at               DateTime            @default(now())
  updated_at               DateTime            @updatedAt
  maintenance_records      AssetMaintenance[]
  transfers                AssetTransfer[]
  depreciation_entries     DepreciationEntry[]
  category                 AssetCategory       @relation(fields: [category_id], references: [id])
  organization             Organization        @relation(fields: [organizationId], references: [id])
  vendor                   Vendor?             @relation(fields: [vendor_id], references: [id])

  @@unique([asset_code, organizationId])
  @@index([organizationId])
  @@index([category_id])
  @@index([status])
  @@map("fixed_assets")
}

model DepreciationEntry {
  id                       String           @id @default(uuid())
  organizationId           String
  asset_id                 String
  depreciation_period      String
  period_start             DateTime
  period_end               DateTime
  opening_book_value       Decimal          @db.Decimal(15, 2)
  depreciation_amount      Decimal          @db.Decimal(15, 2)
  accumulated_depreciation Decimal          @db.Decimal(15, 2)
  closing_book_value       Decimal          @db.Decimal(15, 2)
  calculation_method       String
  journal_entry_id         String?
  status                   String           @default("Draft")
  created_at               DateTime         @default(now())
  updated_at               DateTime         @updatedAt
  asset                    FixedAsset       @relation(fields: [asset_id], references: [id])
  journal_entry            GL_JournalEntry? @relation(fields: [journal_entry_id], references: [id])
  organization             Organization     @relation(fields: [organizationId], references: [id])

  @@unique([asset_id, depreciation_period])
  @@index([organizationId])
  @@index([depreciation_period])
  @@index([status])
  @@map("depreciation_entries")
}

model AssetTransfer {
  id              String       @id @default(uuid())
  organizationId  String
  asset_id        String
  from_location   String?
  to_location     String?
  from_department String?
  to_department   String?
  transfer_date   DateTime
  transfer_reason String?
  approved_by     String?
  created_at      DateTime     @default(now())
  updated_at      DateTime     @updatedAt
  asset           FixedAsset   @relation(fields: [asset_id], references: [id])
  organization    Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([asset_id])
  @@map("asset_transfers")
}

model AssetMaintenance {
  id               String       @id @default(uuid())
  organizationId   String
  asset_id         String
  maintenance_type String
  maintenance_date DateTime
  description      String?
  cost             Decimal?     @db.Decimal(15, 2)
  vendor_id        Int?
  next_due_date    DateTime?
  performed_by     String?
  created_at       DateTime     @default(now())
  updated_at       DateTime     @updatedAt
  asset            FixedAsset   @relation(fields: [asset_id], references: [id])
  organization     Organization @relation(fields: [organizationId], references: [id])
  vendor           Vendor?      @relation(fields: [vendor_id], references: [id])

  @@index([organizationId])
  @@index([asset_id])
  @@index([next_due_date])
  @@map("asset_maintenance")
}

model BudgetMaster {
  id                  String           @id @default(uuid())
  organizationId      String
  budget_name         String
  budget_year         Int
  budget_period       String
  start_date          DateTime
  end_date            DateTime
  status              String           @default("Draft")
  approved_by         String?
  approved_at         DateTime?
  total_budget        Decimal          @default(0) @db.Decimal(15, 2)
  total_actual        Decimal          @default(0) @db.Decimal(15, 2)
  total_variance      Decimal          @default(0) @db.Decimal(15, 2)
  variance_percentage Decimal          @default(0) @db.Decimal(5, 2)
  notes               String?
  created_at          DateTime         @default(now())
  updated_at          DateTime         @updatedAt
  budget_lines        BudgetLine[]
  organization        Organization     @relation(fields: [organizationId], references: [id])
  revisions           BudgetRevision[]

  @@unique([budget_name, budget_year, organizationId])
  @@index([organizationId])
  @@index([budget_year])
  @@index([status])
  @@map("budget_masters")
}

model BudgetLine {
  id                  String           @id @default(uuid())
  organizationId      String
  budget_master_id    String
  department          String?
  cost_center         String?
  account_id          String?
  expense_category_id Int?
  line_description    String
  budget_amount       Decimal          @default(0) @db.Decimal(15, 2)
  actual_amount       Decimal          @default(0) @db.Decimal(15, 2)
  variance            Decimal          @default(0) @db.Decimal(15, 2)
  variance_percentage Decimal          @default(0) @db.Decimal(5, 2)
  alert_threshold     Decimal          @default(90) @db.Decimal(5, 2)
  is_locked           Boolean          @default(false)
  created_at          DateTime         @default(now())
  updated_at          DateTime         @updatedAt
  alerts              BudgetAlert[]
  gl_account          GL_Account?      @relation(fields: [account_id], references: [id])
  budget_master       BudgetMaster     @relation(fields: [budget_master_id], references: [id], onDelete: Cascade)
  expense_category    ExpenseCategory? @relation(fields: [expense_category_id], references: [id])
  organization        Organization     @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([budget_master_id])
  @@index([department])
  @@map("budget_lines")
}

model BudgetRevision {
  id               String       @id @default(uuid())
  organizationId   String
  budget_master_id String
  revision_number  Int
  revision_date    DateTime
  revised_by       String?
  reason           String
  previous_total   Decimal      @db.Decimal(15, 2)
  revised_total    Decimal      @db.Decimal(15, 2)
  variance         Decimal      @db.Decimal(15, 2)
  approved_by      String?
  approved_at      DateTime?
  status           String       @default("Pending")
  created_at       DateTime     @default(now())
  updated_at       DateTime     @updatedAt
  budget_master    BudgetMaster @relation(fields: [budget_master_id], references: [id], onDelete: Cascade)
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([budget_master_id])
  @@map("budget_revisions")
}

model BudgetAlert {
  id                  String       @id @default(uuid())
  organizationId      String
  budget_line_id      String
  alert_type          String
  alert_date          DateTime
  budget_amount       Decimal      @db.Decimal(15, 2)
  actual_amount       Decimal      @db.Decimal(15, 2)
  percentage_consumed Decimal      @db.Decimal(5, 2)
  message             String
  is_acknowledged     Boolean      @default(false)
  acknowledged_by     String?
  acknowledged_at     DateTime?
  created_at          DateTime     @default(now())
  budget_line         BudgetLine   @relation(fields: [budget_line_id], references: [id], onDelete: Cascade)
  organization        Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([budget_line_id])
  @@index([is_acknowledged])
  @@map("budget_alerts")
}

model OTRoom {
  id             String       @id @default(uuid())
  organizationId String
  room_name      String
  room_type      String
  floor          String?
  wing           String?
  is_active      Boolean      @default(true)
  equipment      Json?
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])
  schedules      OTSchedule[]

  @@index([organizationId])
  @@map("ot_rooms")
}

model SurgeryMaster {
  id                    String       @id @default(uuid())
  organizationId        String
  surgery_code          String
  surgery_name          String
  category              String
  sub_category          String?
  default_duration_mins Int          @default(60)
  ot_room_type          String?
  requires_icu          Boolean      @default(false)
  billing_components    Json?
  is_active             Boolean      @default(true)
  created_at            DateTime     @default(now())
  anesthesia_fee        Decimal      @default(0)
  ot_charges            Decimal      @default(0)
  surgeon_fee           Decimal      @default(0)
  organization          Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, surgery_code])
  @@index([organizationId])
  @@map("surgery_masters")
}

model SurgeryRequest {
  id                   String              @id @default(uuid())
  organizationId       String
  request_number       String              @unique
  patient_id           String
  admission_id         String?
  appointment_id       String?
  requesting_doctor_id String
  surgery_master_id    String?
  surgery_name         String
  surgery_category     String?
  urgency              String              @default("Elective")
  clinical_notes       String?
  diagnosis            String?
  icd_codes            Json?
  status               String              @default("Requested")
  requested_date       DateTime?
  approved_by          String?
  approved_at          DateTime?
  cancelled_reason     String?
  created_at           DateTime            @default(now())
  updated_at           DateTime            @updatedAt
  checklist            OTChecklist?
  schedule             OTSchedule?
  pac                  PACClearance?
  billing              SurgeryBilling?
  consumables          SurgeryConsumable[]
  notes                SurgeryNote[]
  organization         Organization        @relation(fields: [organizationId], references: [id])
  team                 SurgeryTeamMember[]

  @@index([organizationId])
  @@index([patient_id])
  @@index([status])
  @@map("surgery_requests")
}

model OTSchedule {
  id                 String         @id @default(uuid())
  organizationId     String
  surgery_request_id String         @unique
  ot_room_id         String
  scheduled_date     DateTime
  start_time         String
  end_time           String
  actual_start       DateTime?
  actual_end         DateTime?
  wheel_in_time      DateTime?
  wheel_out_time     DateTime?
  status             String         @default("Scheduled")
  delay_reason       String?
  created_at         DateTime       @default(now())
  organization       Organization   @relation(fields: [organizationId], references: [id])
  ot_room            OTRoom         @relation(fields: [ot_room_id], references: [id])
  surgery            SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)

  @@index([organizationId, scheduled_date])
  @@index([ot_room_id, scheduled_date])
  @@map("ot_schedules")
}

model SurgeryTeamMember {
  id                 String         @id @default(uuid())
  surgery_request_id String
  role               String
  doctor_id          String?
  staff_name         String
  specialty          String?
  surgery            SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)

  @@index([surgery_request_id])
  @@map("surgery_team_members")
}

model PACClearance {
  id                    String         @id @default(uuid())
  surgery_request_id    String         @unique
  anesthetist_id        String?
  anesthetist_name      String?
  asa_grade             String?
  anesthesia_type       String?
  airway_assessment     Json?
  pre_op_investigations Json?
  fitness_status        String         @default("Pending")
  conditions            String?
  notes                 String?
  cleared_at            DateTime?
  created_at            DateTime       @default(now())
  surgery               SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)

  @@map("pac_clearances")
}

model OTChecklist {
  id                 String         @id @default(uuid())
  surgery_request_id String         @unique
  sign_in            Json?
  time_out           Json?
  sign_out           Json?
  sign_in_by         String?
  sign_in_at         DateTime?
  time_out_by        String?
  time_out_at        DateTime?
  sign_out_by        String?
  sign_out_at        DateTime?
  surgery            SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)

  @@map("ot_checklists")
}

model SurgeryNote {
  id                 String         @id @default(uuid())
  surgery_request_id String
  note_type          String
  content            String
  findings           String?
  complications      String?
  blood_loss_ml      Int?
  duration_mins      Int?
  created_by         String
  created_at         DateTime       @default(now())
  surgery            SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)

  @@index([surgery_request_id])
  @@map("surgery_notes")
}

model SurgeryConsumable {
  id                 String         @id @default(uuid())
  surgery_request_id String
  item_name          String
  item_code          String?
  quantity           Int            @default(1)
  unit_price         Float?
  is_implant         Boolean        @default(false)
  batch_no           String?
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  serial_no          String? // for implants
  item_id            Int?
  store_id           Int?
  movement_id        Int?

  surgery SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)
  item    ItemMaster?    @relation(fields: [item_id], references: [id])
  store   Store?         @relation(fields: [store_id], references: [id])
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  serial_no          String?
  surgery            SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([surgery_request_id])
  @@index([item_id])
  @@index([store_id])
  @@map("surgery_consumables")
}

model SurgeryBilling {
  id                 String         @id @default(uuid())
  surgery_request_id String         @unique
  surgeon_fee        Float          @default(0)
  anesthesia_fee     Float          @default(0)
  ot_charges         Float          @default(0)
  consumable_total   Float          @default(0)
  implant_total      Float          @default(0)
  total_amount       Float          @default(0)
  posted_to_ipd      Boolean        @default(false)
  invoice_id         String?
  surgery            SurgeryRequest @relation(fields: [surgery_request_id], references: [id], onDelete: Cascade)

  @@map("surgery_billings")
}

model ERRegistration {
  id                  String       @id @default(uuid())
  organizationId      String
  er_number           String       @unique
  patient_id          String?
  patient_name        String
  is_unknown          Boolean      @default(false)
  age_estimate        String?
  gender              String?
  brought_by          String?
  arrival_mode        String?
  arrival_time        DateTime     @default(now())
  chief_complaint     String
  triage_level        String?
  triage_color        String?
  triage_nurse_id     String?
  triage_time         DateTime?
  attending_doctor_id String?
  bed_id              String?
  status              String       @default("Triaged")
  is_mlc              Boolean      @default(false)
  disposition         String?
  disposition_time    DateTime?
  admission_id        String?
  created_at          DateTime     @default(now())
  updated_at          DateTime     @updatedAt
  invoice_id          Int?
  er_notes            ERNote[]
  er_orders           EROrder[]
  organization        Organization @relation(fields: [organizationId], references: [id])
  er_vitals           ERVitals[]
  mlc_record          MLCRecord?

  @@index([organizationId])
  @@index([status])
  @@index([triage_level])
  @@map("er_registrations")
}

model MLCRecord {
  id                       String         @id @default(uuid())
  er_registration_id       String         @unique
  mlc_number               String         @unique
  case_type                String
  police_station           String?
  fir_number               String?
  io_name                  String?
  io_contact               String?
  brought_by_name          String?
  brought_by_relation      String?
  brought_by_id_proof      String?
  injury_description       String?
  injury_time              DateTime?
  alcohol_involved         Boolean        @default(false)
  police_informed          Boolean        @default(false)
  police_informed_at       DateTime?
  magistrate_statement     Boolean        @default(false)
  wound_certificate_issued Boolean        @default(false)
  created_at               DateTime       @default(now())
  er_registration          ERRegistration @relation(fields: [er_registration_id], references: [id], onDelete: Cascade)

  @@map("mlc_records")
}

model ERVitals {
  id                 String         @id @default(uuid())
  er_registration_id String
  recorded_at        DateTime       @default(now())
  recorded_by        String
  bp_systolic        Int?
  bp_diastolic       Int?
  heart_rate         Int?
  respiratory_rate   Int?
  temperature        Float?
  spo2               Int?
  gcs_eye            Int?
  gcs_verbal         Int?
  gcs_motor          Int?
  gcs_total          Int?
  pain_scale         Int?
  blood_sugar        Float?
  er_registration    ERRegistration @relation(fields: [er_registration_id], references: [id], onDelete: Cascade)

  @@index([er_registration_id])
  @@map("er_vitals")
}

model EROrder {
  id                 String         @id @default(uuid())
  er_registration_id String
  order_type         String
  order_details      String
  priority           String         @default("Stat")
  status             String         @default("Ordered")
  ordered_by         String
  ordered_at         DateTime       @default(now())
  completed_at       DateTime?
  er_registration    ERRegistration @relation(fields: [er_registration_id], references: [id], onDelete: Cascade)

  @@index([er_registration_id])
  @@map("er_orders")
}

model ERNote {
  id                 String         @id @default(uuid())
  er_registration_id String
  note_type          String
  content            String
  created_by         String
  created_at         DateTime       @default(now())
  er_registration    ERRegistration @relation(fields: [er_registration_id], references: [id], onDelete: Cascade)

  @@index([er_registration_id])
  @@map("er_notes")
}

model Writeoff {
  id               String       @id @default(uuid())
  organizationId   String
  writeoff_number  String       @unique
  patient_id       String
  invoice_id       Int?
  writeoff_type    String
  amount           Decimal      @db.Decimal(15, 2)
  reason           String
  status           String       @default("Requested")
  requested_by     String
  requested_role   String?
  approved_by      String?
  approved_at      DateTime?
  rejected_by      String?
  rejected_at      DateTime?
  rejection_reason String?
  posted_at        DateTime?
  gl_journal_id    String?
  reversed_at      DateTime?
  reversed_by      String?
  reversal_reason  String?
  notes            String?
  approval_chain   Json?
  created_at       DateTime     @default(now())
  updated_at       DateTime     @updatedAt
  invoice          invoices?    @relation(fields: [invoice_id], references: [id])
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([patient_id])
  @@index([status])
  @@index([writeoff_type])
  @@map("writeoffs")
}

model CRMLead {
  id                  String        @id @default(uuid())
  organizationId      String
  lead_number         String        @unique
  patient_id          String?
  name                String
  phone               String
  email               String?
  source              String
  source_detail       String?
  status              String        @default("New")
  assigned_to         String?
  department_interest String?
  doctor_interest     String?
  notes               String?
  last_contacted      DateTime?
  follow_up_date      DateTime?
  converted_at        DateTime?
  lost_reason         String?
  created_at          DateTime      @default(now())
  updated_at          DateTime      @updatedAt
  activities          CRMActivity[]
  organization        Organization  @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([status])
  @@index([source])
  @@index([assigned_to])
}

model CRMActivity {
  id            String   @id @default(uuid())
  lead_id       String
  activity_type String
  direction     String?
  content       String
  outcome       String?
  performed_by  String
  performed_at  DateTime @default(now())
  lead          CRMLead  @relation(fields: [lead_id], references: [id])

  @@index([lead_id])
}

model CRMCampaign {
  id               String       @id @default(uuid())
  organizationId   String
  name             String
  campaign_type    String
  target_audience  String?
  message_template String
  scheduled_at     DateTime?
  status           String       @default("Draft")
  total_recipients Int          @default(0)
  delivered        Int          @default(0)
  opened           Int          @default(0)
  converted        Int          @default(0)
  created_by       String
  created_at       DateTime     @default(now())
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model DoctorReferralNetwork {
  id                String       @id @default(uuid())
  organizationId    String
  doctor_name       String
  specialty         String?
  hospital          String?
  phone             String?
  email             String?
  referral_count    Int          @default(0)
  last_referral     DateTime?
  payout_percentage Float?
  is_active         Boolean      @default(true)
  created_at        DateTime     @default(now())
  organization      Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model PatientEngagement {
  id               String       @id @default(uuid())
  organizationId   String
  patient_id       String
  last_visit       DateTime?
  visit_count      Int          @default(0)
  total_revenue    Float        @default(0)
  engagement_score Int          @default(0)
  risk_level       String?
  next_follow_up   DateTime?
  tags             Json?
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, patient_id])
  @@index([risk_level])
}

model DoctorLeave {
  id             String       @id @default(uuid())
  organizationId String
  doctor_id      String
  doctor_name    String
  leave_type     String       @default("Leave")
  from_date      DateTime
  to_date        DateTime
  reason         String?
  is_approved    Boolean      @default(true)
  approved_by    String?
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([doctor_id])
  @@index([from_date, to_date])
}

model AppointmentWaitlist {
  id                    String       @id @default(uuid())
  organizationId        String
  patient_id            String
  doctor_id             String
  department            String
  preferred_date        DateTime?
  reason                String?
  status                String       @default("Waiting")
  notified_at           DateTime?
  booked_appointment_id String?
  created_at            DateTime     @default(now())
  organization          Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([doctor_id])
  @@index([status])
}

model CallLog {
  id               String       @id @default(uuid())
  organizationId   String
  agent_id         String
  patient_phone    String
  patient_name     String?
  call_type        String
  outcome          String
  appointment_id   String?
  duration_seconds Int?
  notes            String?
  created_at       DateTime     @default(now())
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([agent_id])
  @@index([patient_phone])
}

model AdmissionBooking {
  id             String       @id @default(uuid())
  organizationId String
  patient_id     String
  booking_number String       @unique
  expected_date  DateTime
  bed_category   String       @default("General")
  department     String
  doctor_id      String?
  doctor_name    String?
  estimated_cost Float?
  deposit_paid   Float?       @default(0)
  status         String       @default("Booked")
  notes          String?
  created_at     DateTime     @default(now())
  admission_type String       @default("REGULAR")
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([patient_id])
  @@index([expected_date])
  @@index([admission_type])
}

model PatientMovement {
  id             String       @id @default(uuid())
  organizationId String
  admission_id   String
  patient_id     String
  from_location  String
  to_location    String
  purpose        String?
  moved_at       DateTime     @default(now())
  returned_at    DateTime?
  moved_by       String?
  escort_name    String?
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
}

model DischargeClearance {
  id             String       @id @default(uuid())
  organizationId String
  admission_id   String       @unique
  pharmacy       String       @default("Pending")
  pharmacy_by    String?
  pharmacy_at    DateTime?
  lab            String       @default("Pending")
  lab_by         String?
  lab_at         DateTime?
  finance        String       @default("Pending")
  finance_by     String?
  finance_at     DateTime?
  nursing        String       @default("Pending")
  nursing_by     String?
  nursing_at     DateTime?
  doctor         String       @default("Pending")
  doctor_by      String?
  doctor_at      DateTime?
  all_cleared    Boolean      @default(false)
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
}

model PatientConsent_IPD {
  id             String       @id @default(uuid())
  organizationId String
  admission_id   String
  consent_type   String
  form_url       String?
  signed_at      DateTime?
  witness_name   String?
  notes          String?
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([admission_id])
}

model CounsellingSession {
  id                 String       @id @default(uuid())
  organizationId     String
  session_number     String       @unique
  patient_id         String
  counsellor_id      String?
  counsellor_name    String?
  session_type       String       @default("General")
  status             String       @default("Requested")
  scheduled_at       DateTime?
  completed_at       DateTime?
  notes              String?
  outcome            String?
  financial_estimate Float?
  deposit_advised    Float?
  patient_consent    Boolean      @default(false)
  created_at         DateTime     @default(now())
  updated_at         DateTime     @updatedAt
  organization       Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([patient_id])
  @@index([status])
}

model FinancialCounselling {
  id              String       @id @default(uuid())
  organizationId  String
  patient_id      String
  counsellor_id   String?
  counsellor_name String?
  invoice_id      String?
  estimated_cost  Float?
  deposit_advised Float?
  payment_plan    String?
  notes           String?
  status          String       @default("Pending")
  created_at      DateTime     @default(now())
  updated_at      DateTime     @updatedAt
  organization    Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([patient_id])
}

model RegistrationFieldConfig {
  id             String       @id @default(uuid())
  organizationId String
  field_name     String
  is_required    Boolean      @default(false)
  is_visible     Boolean      @default(true)
  display_label  String?
  sort_order     Int          @default(0)
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, field_name])
  @@index([organizationId])
}

model DiscountOTP {
  id               String       @id @default(uuid())
  organizationId   String
  invoice_id       String
  discount_amount  Float
  discount_percent Float?
  otp_code         String
  requested_by     String
  approved_by      String?
  expires_at       DateTime
  used_at          DateTime?
  status           String       @default("Pending")
  created_at       DateTime     @default(now())
  organization     Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([invoice_id])
}

model BillingOrderSet {
  id             String       @id @default(uuid())
  organizationId String
  name           String
  category       String?
  description    String?
  items          Json
  total_amount   Float        @default(0)
  is_active      Boolean      @default(true)
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model DiscountScheme {
  id             String       @id @default(uuid())
  organizationId String
  name           String
  scheme_type    String
  value          Float
  applicable_to  Json?
  valid_from     DateTime?
  valid_to       DateTime?
  requires_otp   Boolean      @default(false)
  is_active      Boolean      @default(true)
  created_at     DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model PharmacyInventoryMovement {
  id             Int                       @id @default(autoincrement())
  organizationId String
  medicine_id    Int
  batch_id       Int?
  movement_type  String
  quantity_in    Int                       @default(0)
  quantity_out   Int                       @default(0)
  unit_cost      Float?
  balance_after  Int
  source_type    String?
  source_id      String?
  user_id        String?
  reason         String?
  created_at     DateTime                  @default(now())
  batch          pharmacy_batch_inventory? @relation(fields: [batch_id], references: [id])
  medicine       pharmacy_medicine_master  @relation(fields: [medicine_id], references: [id])
  organization   Organization              @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([medicine_id])
  @@index([batch_id])
  @@index([movement_type])
  @@index([created_at])
  @@map("pharmacy_inventory_movements")
}

model PharmacyPurchaseInvoice {
  id                Int                           @id @default(autoincrement())
  organizationId    String
  invoice_number    String
  vendor_id         Int
  po_id             Int?
  invoice_date      DateTime
  due_date          DateTime?
  subtotal          Float                         @default(0)
  cgst_amount       Float                         @default(0)
  sgst_amount       Float                         @default(0)
  igst_amount       Float                         @default(0)
  total_amount      Float                         @default(0)
  amount_paid       Float                         @default(0)
  tds_amount        Float                         @default(0)
  status            String                        @default("Draft")
  variance_approved Boolean                       @default(false)
  matching_notes    String?
  gl_posted         Boolean                       @default(false)
  vendor_gstin      String?
  created_at        DateTime                      @default(now())
  updated_at        DateTime                      @updatedAt
  line_items        PharmacyPurchaseInvoiceLine[]
  organization      Organization                  @relation(fields: [organizationId], references: [id])
  po                PurchaseOrder?                @relation(fields: [po_id], references: [id])
  vendor            Vendor                        @relation(fields: [vendor_id], references: [id])

  @@unique([invoice_number, organizationId])
  @@index([organizationId])
  @@index([vendor_id])
  @@index([status])
  @@map("pharmacy_purchase_invoices")
}

model PharmacyPurchaseInvoiceLine {
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id              Int    @id @default(autoincrement())
  invoice_id      Int
  medicine_id     Int?
  item_id         Int?
  grn_id          Int?
  po_item_id      Int?
  quantity        Int
  unit_price      Float
  gst_rate        Float  @default(0)
  cgst_amount     Float  @default(0)
  sgst_amount     Float  @default(0)
  igst_amount     Float  @default(0)
  line_total      Float
  hsn_code        String?

  invoice  PharmacyPurchaseInvoice  @relation(fields: [invoice_id], references: [id])
  medicine pharmacy_medicine_master? @relation(fields: [medicine_id], references: [id])
  item     ItemMaster?              @relation(fields: [item_id], references: [id])
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
  id          Int                      @id @default(autoincrement())
  invoice_id  Int
  medicine_id Int
  grn_id      Int?
  po_item_id  Int?
  quantity    Int
  unit_price  Float
  gst_rate    Float                    @default(0)
  cgst_amount Float                    @default(0)
  sgst_amount Float                    @default(0)
  igst_amount Float                    @default(0)
  line_total  Float
  hsn_code    String?
  invoice     PharmacyPurchaseInvoice  @relation(fields: [invoice_id], references: [id])
  medicine    pharmacy_medicine_master @relation(fields: [medicine_id], references: [id])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c

  @@index([invoice_id])
  @@index([item_id])
  @@map("pharmacy_purchase_invoice_lines")
}

model DispenseAllocation {
  id             Int                      @id @default(autoincrement())
  organizationId String
  order_item_id  Int
  batch_id       Int
  quantity       Int
  unit_cost      Float
  created_at     DateTime                 @default(now())
  batch          pharmacy_batch_inventory @relation(fields: [batch_id], references: [id])
  order_item     pharmacy_order_items     @relation(fields: [order_item_id], references: [id])
  organization   Organization             @relation(fields: [organizationId], references: [id])

  @@index([order_item_id])
  @@index([batch_id])
  @@map("dispense_allocations")
}

<<<<<<< HEAD
// =================================
// MODULE — General Inventory & Materials Management
// =================================
=======
<<<<<<< HEAD
// ========================================
// MODULE — General Inventory & Materials Management
// ========================================
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a

model ItemCategory {
  id                      Int          @id @default(autoincrement())
  name                    String
  parent_id               Int?
  item_type               String       // CONSUMABLE, REAGENT, IMPLANT, LINEN, STATIONERY, MAINTENANCE, EQUIPMENT_SPARE, FOOD_DIETARY, OTHER
  gl_inventory_account_id String?      // FK to GL_Account
  gl_expense_account_id   String?      // FK to GL_Account
  gl_cogs_account_id      String?      // FK to GL_Account
  default_gst_rate        Float        @default(0)
  organizationId          String
  created_at              DateTime     @default(now())
  updated_at              DateTime     @updatedAt

  organization            Organization @relation(fields: [organizationId], references: [id])
  parent                  ItemCategory? @relation("CategoryHierarchy", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  children                ItemCategory[] @relation("CategoryHierarchy")
  items                   ItemMaster[]
  gl_inventory_account    GL_Account?  @relation("CategoryInventoryAccount", fields: [gl_inventory_account_id], references: [id])
  gl_expense_account      GL_Account?  @relation("CategoryExpenseAccount", fields: [gl_expense_account_id], references: [id])
  gl_cogs_account         GL_Account?  @relation("CategoryCOGSAccount", fields: [gl_cogs_account_id], references: [id])

  @@unique([name, organizationId])
  @@index([organizationId])
  @@map("item_categories")
}

model ItemMaster {
  id                   Int          @id @default(autoincrement())
  item_code            String
  name                 String
  description          String?
  category_id          Int
  item_type            String       // CONSUMABLE, REAGENT, IMPLANT, LINEN, STATIONERY, MAINTENANCE, EQUIPMENT_SPARE, FOOD_DIETARY, OTHER
  base_uom             String
  purchase_uom         String
  uom_conversion       Float        @default(1.0)
  hsn_sac_code         String?
  gst_rate             Float        @default(0)
  std_purchase_price   Float        @default(0)
  selling_price        Float        @default(0)
  mrp                  Float        @default(0)
  is_batch_tracked     Boolean      @default(false)
  is_expiry_tracked    Boolean      @default(false)
  is_patient_chargeable Boolean     @default(false)
  charge_catalog_id    Int?         // FK to charge_catalog
  is_returnable        Boolean      @default(true)
  is_cold_chain        Boolean      @default(false)
  abc_class            String?      // A, B, C
  double_check_rop     Boolean      @default(false) // placeholder config
  ved_class            String?      // V, E, D
  min_level            Int          @default(0)
  max_level            Int          @default(0)
  reorder_point        Int          @default(0)
  lead_time_days       Int          @default(0)
  barcode              String?
  status               String       @default("Active") // Draft, Active, Discontinued
  organizationId       String
  created_at           DateTime     @default(now())
  updated_at           DateTime     @updatedAt

  organization         Organization @relation(fields: [organizationId], references: [id])
  category             ItemCategory @relation(fields: [category_id], references: [id])
  charge_catalog       charge_catalog? @relation(fields: [charge_catalog_id], references: [id])
  
  vendors              ItemVendor[]
  batches              ItemBatch[]
  store_stocks         StoreStock[]
  store_settings       StoreItemSetting[]
  movements            InventoryMovement[]
  purchase_order_items PurchaseOrderItem[]
  requisition_items    PurchaseRequisitionItem[]
  indent_items         IndentItem[]
  stock_issue_items    StockIssueItem[]
  stock_transfer_items StockTransferItem[]
  count_lines          StockCountLine[]
  surgery_consumables  SurgeryConsumable[]
  purchase_invoice_lines PharmacyPurchaseInvoiceLine[]
  pharmacy_returns     PharmacyReturn[]
  grn_items            GoodsReceiptNoteItem[]

  @@unique([item_code, organizationId])
  @@index([organizationId])
  @@index([category_id])
  @@map("item_master")
}

model ItemVendor {
  id                Int          @id @default(autoincrement())
  item_id           Int
  vendor_id         Int
  vendor_item_code  String?
  last_price        Float        @default(0)
  preference_rank   Int          @default(1)
  organizationId    String
  created_at        DateTime     @default(now())
  updated_at        DateTime     @updatedAt

  organization      Organization @relation(fields: [organizationId], references: [id])
  item              ItemMaster   @relation(fields: [item_id], references: [id], onDelete: Cascade)
  vendor            Vendor       @relation(fields: [vendor_id], references: [id], onDelete: Cascade)

  @@unique([item_id, vendor_id])
  @@index([organizationId])
  @@map("item_vendors")
}

model Store {
  id                Int          @id @default(autoincrement())
  store_code        String
  name              String
  store_type        String       // CENTRAL, PHARMACY, WARD, OT, ER, LAB, RADIOLOGY, MAINTENANCE, HOUSEKEEPING, KITCHEN, CSSD
  branch_id         String?
  parent_store_id   Int?
  cost_center       String?
  incharge_user_id  String?
  is_active         Boolean      @default(true)
  organizationId    String
  created_at        DateTime     @default(now())
  updated_at        DateTime     @updatedAt

  organization      Organization @relation(fields: [organizationId], references: [id])
  branch            Branch?      @relation(fields: [branch_id], references: [id])
  parent_store      Store?       @relation("StoreReplenishment", fields: [parent_store_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  child_stores      Store[]      @relation("StoreReplenishment")
  incharge_user     User?        @relation(fields: [incharge_user_id], references: [id])

  store_stocks      StoreStock[]
  store_settings    StoreItemSetting[]
  movements         InventoryMovement[]
  purchase_orders   PurchaseOrder[]      @relation("StorePurchaseOrders")
  grns              GoodsReceiptNote[]   @relation("StoreGRNs")
  requisitions      PurchaseRequisition[] @relation("StoreRequisitions")
  indents_from      Indent[]             @relation("IndentFromStore")
  indents_to        Indent[]             @relation("IndentToStore")
  issues_from       StockIssue[]         @relation("IssueFromStore")
  issues_to         StockIssue[]         @relation("IssueToStore")
  transfers_from    StockTransfer[]      @relation("TransferFromStore")
  transfers_to      StockTransfer[]      @relation("TransferToStore")
  count_sessions    StockCountSession[]
  adjustments       StockAdjustment[]
  surgery_consumables SurgeryConsumable[]
  pharmacy_returns  PharmacyReturn[]

  @@unique([store_code, organizationId])
  @@index([organizationId])
  @@map("stores")
}

model StoreItemSetting {
  id             Int          @id @default(autoincrement())
  store_id       Int
  item_id        Int
  par_level      Int          @default(0)
  reorder_point  Int          @default(0)
  max_level      Int          @default(0)
  auto_indent    Boolean      @default(false)
  organizationId String
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt

  organization   Organization @relation(fields: [organizationId], references: [id])
  store          Store        @relation(fields: [store_id], references: [id], onDelete: Cascade)
  item           ItemMaster   @relation(fields: [item_id], references: [id], onDelete: Cascade)

  @@unique([store_id, item_id])
  @@index([organizationId])
  @@map("store_item_settings")
}

model ItemBatch {
  id              Int          @id @default(autoincrement())
  item_id         Int
  batch_no        String
  expiry_date     DateTime?
  mfg_date        DateTime?
  mrp             Float?
  cost_price      Float        @default(0)
  grn_id          Int?
  vendor_id       Int?
  is_quarantined  Boolean      @default(false)
  organizationId  String
  created_at      DateTime     @default(now())
  updated_at      DateTime     @updatedAt

  organization    Organization @relation(fields: [organizationId], references: [id])
  item            ItemMaster   @relation(fields: [item_id], references: [id], onDelete: Cascade)
  grn             GoodsReceiptNote? @relation(fields: [grn_id], references: [id])
  vendor          Vendor?      @relation(fields: [vendor_id], references: [id])

  store_stocks    StoreStock[]
  movements       InventoryMovement[]
  issue_items     StockIssueItem[]
  count_lines     StockCountLine[]
  transfer_items  StockTransferItem[]

  @@unique([item_id, batch_no])
  @@index([organizationId])
  @@map("item_batches")
}

model StoreStock {
  id               Int          @id @default(autoincrement())
  store_id         Int
  item_id          Int
  batch_id         Int?
  quantity_on_hand Int          @default(0)
  quantity_reserved Int         @default(0)
  avg_unit_cost    Float        @default(0)
  bin_location     String?
  organizationId   String
  created_at       DateTime     @default(now())
  updated_at       DateTime     @updatedAt

  organization     Organization @relation(fields: [organizationId], references: [id])
  store            Store        @relation(fields: [store_id], references: [id], onDelete: Cascade)
  item             ItemMaster   @relation(fields: [item_id], references: [id], onDelete: Cascade)
  batch            ItemBatch?   @relation(fields: [batch_id], references: [id])

  @@unique([store_id, item_id, batch_id])
  @@index([organizationId])
  @@map("store_stocks")
}

model InventoryMovement {
  id              Int          @id @default(autoincrement())
  organizationId  String
  store_id        Int
  item_id         Int
  batch_id        Int?
  movement_type   String       // OPENING, GRN_RECEIPT, ISSUE, INDENT_ISSUE, INDENT_RECEIPT, TRANSFER_OUT, TRANSFER_IN, CONSUMPTION, PATIENT_CHARGE, RETURN_TO_STORE, SUPPLIER_RETURN, EXPIRY_WRITEOFF, DAMAGE_WRITEOFF, ADJUSTMENT_PLUS, ADJUSTMENT_MINUS
  quantity_in     Int          @default(0)
  quantity_out    Int          @default(0)
  unit_cost       Float        @default(0)
  value           Float        @default(0)
  balance_after   Int
  source_type     String?      // PR, PO, GRN, INDENT, ISSUE, TRANSFER, ADJUSTMENT, SURGERY, CONSUMPTION
  source_id       String?      // Can be Int or String UUID, stored as String
  cost_center     String?
  patient_id      String?      // for patient charges
  admission_id    String?      // for patient charges
  user_id         String?      // operator
  reason          String?
  gl_journal_id   String?      // FK to GL_JournalEntry
  created_at      DateTime     @default(now())

  organization    Organization @relation(fields: [organizationId], references: [id])
  store           Store        @relation(fields: [store_id], references: [id])
  item            ItemMaster   @relation(fields: [item_id], references: [id])
  batch           ItemBatch?   @relation(fields: [batch_id], references: [id])
  gl_journal      GL_JournalEntry? @relation(fields: [gl_journal_id], references: [id])
  patient         admissions?  @relation(fields: [admission_id], references: [admission_id])

  @@index([organizationId])
  @@index([store_id])
  @@index([item_id])
  @@index([batch_id])
  @@index([movement_type])
  @@index([created_at])
  @@map("inventory_movements")
}

model PurchaseRequisition {
  id                 Int                       @id @default(autoincrement())
  pr_number          String                    @unique
  requesting_store_id Int
  status             String                    @default("Draft") // Draft, Submitted, Approved, Rejected, Converted, Cancelled
  priority           String                    @default("NORMAL") // NORMAL, URGENT, EMERGENCY
  approved_by        String?
  approved_at        DateTime?
  organizationId     String
  created_at         DateTime                  @default(now())
  updated_at         DateTime                  @updatedAt

  organization       Organization              @relation(fields: [organizationId], references: [id])
  store              Store                     @relation("StoreRequisitions", fields: [requesting_store_id], references: [id])
  items              PurchaseRequisitionItem[]
  purchase_orders    PurchaseOrder[]

  @@index([organizationId])
  @@map("purchase_requisitions")
}

model PurchaseRequisitionItem {
  id             Int                 @id @default(autoincrement())
  pr_id          Int
  item_id        Int
  quantity       Int
  required_by    DateTime?
  notes          String?

  requisition    PurchaseRequisition @relation(fields: [pr_id], references: [id], onDelete: Cascade)
  item           ItemMaster          @relation(fields: [item_id], references: [id])

  @@index([pr_id])
  @@map("purchase_requisition_items")
}

model Indent {
  id              Int          @id @default(autoincrement())
  indent_number   String       @unique
  from_store_id   Int          // requesting store
  to_store_id     Int          // issuing store (parent or central)
  cost_center     String?
  priority        String       @default("NORMAL") // NORMAL, URGENT, EMERGENCY
  patient_id      String?      // optional patient/admission reference
  admission_id    String?
  status          String       @default("Draft") // Draft, Submitted, Approved, Partially Issued, Issued, Closed, Rejected, Cancelled
  approved_by     String?
  approved_at     DateTime?
  organizationId  String
  created_at      DateTime     @default(now())
  updated_at      DateTime     @updatedAt

  organization    Organization @relation(fields: [organizationId], references: [id])
  from_store      Store        @relation("IndentFromStore", fields: [from_store_id], references: [id])
  to_store        Store        @relation("IndentToStore", fields: [to_store_id], references: [id])
  patient         admissions?  @relation(fields: [admission_id], references: [admission_id])

  items           IndentItem[]
  issues          StockIssue[]

  @@index([organizationId])
  @@map("indents")
}

model IndentItem {
  id             Int          @id @default(autoincrement())
  indent_id      Int
  item_id        Int
  qty_requested  Int
  qty_approved   Int          @default(0)
  qty_issued     Int          @default(0)
  qty_received   Int          @default(0)

  indent         Indent       @relation(fields: [indent_id], references: [id], onDelete: Cascade)
  item           ItemMaster   @relation(fields: [item_id], references: [id])

  @@index([indent_id])
  @@map("indent_items")
}

model StockIssue {
  id             Int              @id @default(autoincrement())
  issue_number   String           @unique
  indent_id      Int?
  from_store_id  Int              // issuing store
  to_store_id    Int?             // receiving store
  cost_center    String?          // if direct consumption issue
  organizationId String
  created_at     DateTime         @default(now())
  updated_at     DateTime         @updatedAt

  organization   Organization     @relation(fields: [organizationId], references: [id])
  indent         Indent?          @relation(fields: [indent_id], references: [id])
  from_store     Store            @relation("IssueFromStore", fields: [from_store_id], references: [id])
  to_store       Store?           @relation("IssueToStore", fields: [to_store_id], references: [id])
  items          StockIssueItem[]

  @@index([organizationId])
  @@map("stock_issues")
}

model StockIssueItem {
  id             Int            @id @default(autoincrement())
  issue_id       Int
  item_id        Int
  batch_id       Int?
  quantity       Int
  unit_cost      Float          @default(0)

  issue          StockIssue     @relation(fields: [issue_id], references: [id], onDelete: Cascade)
  item           ItemMaster     @relation(fields: [item_id], references: [id])
  batch          ItemBatch?     @relation(fields: [batch_id], references: [id])

  @@index([issue_id])
  @@map("stock_issue_items")
}

model StockTransfer {
  id              Int                 @id @default(autoincrement())
  transfer_number String              @unique
  from_store_id   Int
  to_store_id     Int
  status          String              @default("Draft") // Draft, Dispatched, In Transit, Received, Cancelled
  dispatch_user_id String?
  dispatch_at     DateTime?
  receive_user_id String?
  received_at     DateTime?
  organizationId  String
  created_at      DateTime            @default(now())
  updated_at      DateTime            @updatedAt

  organization    Organization        @relation(fields: [organizationId], references: [id])
  from_store      Store               @relation("TransferFromStore", fields: [from_store_id], references: [id])
  to_store        Store               @relation("TransferToStore", fields: [to_store_id], references: [id])
  dispatch_user   User?               @relation("TransferDispatchUser", fields: [dispatch_user_id], references: [id])
  receive_user    User?               @relation("TransferReceiveUser", fields: [receive_user_id], references: [id])

  items           StockTransferItem[]

  @@index([organizationId])
  @@map("stock_transfers")
}

model StockTransferItem {
  id             Int           @id @default(autoincrement())
  transfer_id    Int
  item_id        Int
  batch_id       Int?
  quantity       Int
  unit_cost      Float         @default(0)

  transfer       StockTransfer @relation(fields: [transfer_id], references: [id], onDelete: Cascade)
  item           ItemMaster    @relation(fields: [item_id], references: [id])
  batch          ItemBatch?    @relation(fields: [batch_id], references: [id])

  @@index([transfer_id])
  @@map("stock_transfer_items")
}

model StockCountSession {
  id             Int              @id @default(autoincrement())
  session_number String           @unique
  store_id       Int
  status         String           @default("Draft") // Draft, Frozen, Counting, Review, Approved, Posted
  frozen_at      DateTime?
  approved_by    String?
  approved_at    DateTime?
  organizationId String
  created_at     DateTime         @default(now())
  updated_at     DateTime         @updatedAt

  organization   Organization     @relation(fields: [organizationId], references: [id])
  store          Store            @relation(fields: [store_id], references: [id])
  lines          StockCountLine[]
  adjustments    StockAdjustment[]

  @@index([organizationId])
  @@map("stock_count_sessions")
}

model StockCountLine {
  id             Int               @id @default(autoincrement())
  session_id     Int
  item_id        Int
  batch_id       Int?
  book_qty       Int               @default(0)
  counted_qty    Int               @default(0)
  recount_qty    Int?
  variance_value Float             @default(0)
  recount_requested Boolean        @default(false)

  session        StockCountSession @relation(fields: [session_id], references: [id], onDelete: Cascade)
  item           ItemMaster        @relation(fields: [item_id], references: [id])
  batch          ItemBatch?        @relation(fields: [batch_id], references: [id])

  @@index([session_id])
  @@map("stock_count_lines")
}

model StockAdjustment {
  id                Int               @id @default(autoincrement())
  adjustment_number String            @unique
  store_id          Int
  count_session_id  Int?
  reason_code       String            // DAMAGE, EXPIRY, COUNT_VARIANCE, THEFT_LOSS, DATA_CORRECTION
  status            String            @default("Pending") // Pending, Approved, Posted, Rejected
  approved_by       String?
  approved_at       DateTime?
  gl_posted         Boolean           @default(false)
  organizationId    String
  created_at        DateTime          @default(now())
  updated_at        DateTime          @updatedAt

  organization      Organization      @relation(fields: [organizationId], references: [id])
  store             Store             @relation(fields: [store_id], references: [id])
  count_session     StockCountSession? @relation(fields: [count_session_id], references: [id])

  @@index([organizationId])
  @@map("stock_adjustments")
}

model GoodsReceiptNoteItem {
  id                Int              @id @default(autoincrement())
  grn_id            Int
  item_id           Int
  quantity_accepted Int
  quantity_rejected Int              @default(0)
  rejection_reason  String?
  batch_no          String?
  expiry_date       DateTime?
  unit_price        Float            @default(0)
  gst_rate          Float            @default(0)

  grn               GoodsReceiptNote @relation(fields: [grn_id], references: [id], onDelete: Cascade)
  item              ItemMaster       @relation(fields: [item_id], references: [id])

  @@index([grn_id])
  @@map("goods_receipt_note_items")
<<<<<<< HEAD

=======
=======
>>>>>>> ac65a1c0df7665e61b0298cf6a76384e7d1a588a
model ReportJob {
  id             String       @id @default(uuid())
  job_number     Int          @default(autoincrement())
  report_id      String
  requested_by   String
  filters_json   Json
  status         String       @default("Queued")
  progress       Int          @default(0)
  row_count      Int?
  file_key       String?
  format         String
  error          String?
  started_at     DateTime?
  finished_at    DateTime?
  expires_at     DateTime?
  organizationId String
  createdAt      DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])
  user           User         @relation("ReportJobRequestedBy", fields: [requested_by], references: [id])

  @@index([organizationId])
  @@index([requested_by])
  @@map("report_jobs")
}

model ReportPreset {
  id             String           @id @default(uuid())
  report_id      String
  user_id        String
  name           String
  filters_json   Json
  is_shared      Boolean          @default(false)
  organizationId String
  createdAt      DateTime         @default(now())
  organization   Organization     @relation(fields: [organizationId], references: [id])
  user           User             @relation(fields: [user_id], references: [id])
  schedules      ReportSchedule[]

  @@index([organizationId])
  @@index([user_id])
  @@map("report_presets")
}

model ReportSchedule {
  id              String       @id @default(uuid())
  report_id       String
  preset_id       String
  cron_spec       String
  format          String
  recipients_json Json
  channel         String
  is_active       Boolean      @default(true)
  last_run_at     DateTime?
  last_status     String?
  owner_user_id   String
  organizationId  String
  createdAt       DateTime     @default(now())
  organization    Organization @relation(fields: [organizationId], references: [id])
  owner_user      User         @relation("ReportScheduleOwner", fields: [owner_user_id], references: [id])
  preset          ReportPreset @relation(fields: [preset_id], references: [id])

  @@index([organizationId])
  @@map("report_schedules")
}

model ReportAccessLog {
  id             String       @id @default(uuid())
  user_id        String
  report_id      String
  filters_json   Json
  row_count      Int?
  format         String?
  action         String
  organizationId String
  createdAt      DateTime     @default(now())
  organization   Organization @relation(fields: [organizationId], references: [id])
  user           User         @relation(fields: [user_id], references: [id])

  @@index([organizationId])
  @@map("report_access_logs")
}

model indent_items {
  id           Int         @id @default(autoincrement())
  indentId     Int
  itemId       Int
  qtyRequested Decimal
  qtyApproved  Decimal?
  qtyIssued    Decimal     @default(0)
  qtyReceived  Decimal     @default(0)
  indents      indents     @relation(fields: [indentId], references: [id])
  item_master  item_master @relation(fields: [itemId], references: [id])
}

model indents {
  id                                 Int            @id @default(autoincrement())
  indentNumber                       String         @unique
  fromStoreId                        Int
  toStoreId                          Int
  costCenter                         String?
  priority                           String         @default("NORMAL")
  patientId                          Int?
  admissionId                        Int?
  status                             String         @default("DRAFT")
  approvedById                       String?
  organizationId                     String
  createdAt                          DateTime       @default(now())
  updatedAt                          DateTime
  indent_items                       indent_items[]
  users                              User?          @relation(fields: [approvedById], references: [id])
  stores_indents_fromStoreIdTostores stores         @relation("indents_fromStoreIdTostores", fields: [fromStoreId], references: [id])
  organizations                      Organization   @relation(fields: [organizationId], references: [id])
  stores_indents_toStoreIdTostores   stores         @relation("indents_toStoreIdTostores", fields: [toStoreId], references: [id])
  stock_issues                       stock_issues[]

  @@index([organizationId, status])
}

model inventory_movements {
  id             Int           @id @default(autoincrement())
  storeId        Int
  itemId         Int
  batchId        Int?
  movementType   String
  quantityIn     Decimal       @default(0)
  quantityOut    Decimal       @default(0)
  unitCost       Decimal
  value          Decimal
  balanceAfter   Decimal
  sourceType     String?
  sourceId       Int?
  costCenter     String?
  patientId      Int?
  admissionId    Int?
  userId         String
  reason         String?
  glJournalId    String?
  organizationId String
  createdAt      DateTime      @default(now())
  item_batches   item_batches? @relation(fields: [batchId], references: [id])
  item_master    item_master   @relation(fields: [itemId], references: [id])
  organizations  Organization  @relation(fields: [organizationId], references: [id])
  stores         stores        @relation(fields: [storeId], references: [id])
  users          User          @relation(fields: [userId], references: [id])

  @@index([glJournalId])
  @@index([organizationId])
  @@index([storeId, itemId, createdAt])
}

model item_batches {
  id                   Int                    @id @default(autoincrement())
  itemId               Int
  batchNo              String
  expiryDate           DateTime?
  mfgDate              DateTime?
  mrp                  Decimal?
  costPrice            Decimal
  grnId                Int?
  vendorId             Int?
  isQuarantined        Boolean                @default(false)
  organizationId       String
  createdAt            DateTime               @default(now())
  inventory_movements  inventory_movements[]
  item_master          item_master            @relation(fields: [itemId], references: [id])
  organizations        Organization           @relation(fields: [organizationId], references: [id])
  vendors              Vendor?                @relation(fields: [vendorId], references: [id])
  stock_count_lines    stock_count_lines[]
  stock_issue_items    stock_issue_items[]
  stock_transfer_items stock_transfer_items[]
  store_stock          store_stock[]

  @@unique([itemId, batchNo])
  @@index([expiryDate])
  @@index([organizationId])
}

model item_categories {
  id                    Int               @id @default(autoincrement())
  name                  String
  parentId              Int?
  itemType              String
  glInventoryAccountId  String?
  glExpenseAccountId    String?
  glCogsAccountId       String?
  defaultGstRate        Decimal           @default(0)
  organizationId        String
  createdAt             DateTime          @default(now())
  updatedAt             DateTime
  organizations         Organization      @relation(fields: [organizationId], references: [id])
  item_categories       item_categories?  @relation("item_categoriesToitem_categories", fields: [parentId], references: [id])
  other_item_categories item_categories[] @relation("item_categoriesToitem_categories")
  item_master           item_master[]

  @@index([organizationId])
}

model item_master {
  id                         Int                          @id @default(autoincrement())
  itemCode                   String
  name                       String
  description                String?
  categoryId                 Int
  itemType                   String
  baseUom                    String
  purchaseUom                String
  uomConversion              Decimal                      @default(1)
  hsnSacCode                 String?
  gstRate                    Decimal                      @default(0)
  stdPurchasePrice           Decimal?
  sellingPrice               Decimal?
  mrp                        Decimal?
  isBatchTracked             Boolean                      @default(false)
  isExpiryTracked            Boolean                      @default(false)
  isPatientChargeable        Boolean                      @default(false)
  chargeCatalogId            Int?
  isReturnable               Boolean                      @default(false)
  isColdChain                Boolean                      @default(false)
  abcClass                   String?
  vedClass                   String?
  minLevel                   Decimal?
  maxLevel                   Decimal?
  reorderPoint               Decimal?
  leadTimeDays               Int?
  barcode                    String?
  status                     String                       @default("DRAFT")
  specAttachmentUrl          String?
  organizationId             String
  createdAt                  DateTime                     @default(now())
  updatedAt                  DateTime
  indent_items               indent_items[]
  inventory_movements        inventory_movements[]
  item_batches               item_batches[]
  item_categories            item_categories              @relation(fields: [categoryId], references: [id])
  organizations              Organization                 @relation(fields: [organizationId], references: [id])
  item_vendors               item_vendors[]
  purchase_order_items       PurchaseOrderItem[]
  purchase_requisition_items purchase_requisition_items[]
  stock_count_lines          stock_count_lines[]
  stock_issue_items          stock_issue_items[]
  stock_transfer_items       stock_transfer_items[]
  store_item_settings        store_item_settings[]
  store_stock                store_stock[]

  @@unique([itemCode, organizationId])
  @@index([organizationId])
  @@index([status, organizationId])
}

model item_vendors {
  id             Int         @id @default(autoincrement())
  itemId         Int
  vendorId       Int
  vendorItemCode String?
  lastPrice      Decimal?
  preferenceRank Int         @default(1)
  organizationId String
  createdAt      DateTime    @default(now())
  item_master    item_master @relation(fields: [itemId], references: [id])
  vendors        Vendor      @relation(fields: [vendorId], references: [id])

  @@index([organizationId])
}

model purchase_requisition_items {
  id                    Int                   @id @default(autoincrement())
  prId                  Int
  itemId                Int
  quantity              Decimal
  uom                   String
  requiredBy            DateTime?
  notes                 String?
  item_master           item_master           @relation(fields: [itemId], references: [id])
  purchase_requisitions purchase_requisitions @relation(fields: [prId], references: [id])
}

model purchase_requisitions {
  id                         Int                          @id @default(autoincrement())
  prNumber                   String                       @unique
  requestingStoreId          Int
  status                     String                       @default("DRAFT")
  priority                   String                       @default("NORMAL")
  approvedById               String?
  approvedAt                 DateTime?
  notes                      String?
  organizationId             String
  createdAt                  DateTime                     @default(now())
  updatedAt                  DateTime
  purchase_requisition_items purchase_requisition_items[]
  users                      User?                        @relation(fields: [approvedById], references: [id])
  organizations              Organization                 @relation(fields: [organizationId], references: [id])
  stores                     stores                       @relation(fields: [requestingStoreId], references: [id])

  @@index([organizationId, status])
}

model stock_adjustments {
  id                   Int                   @id @default(autoincrement())
  adjustmentNumber     String                @unique
  storeId              Int
  countSessionId       Int?
  reasonCode           String
  status               String                @default("PENDING")
  approvedById         String?
  glPosted             Boolean               @default(false)
  organizationId       String
  createdAt            DateTime              @default(now())
  stock_count_sessions stock_count_sessions? @relation(fields: [countSessionId], references: [id])
  organizations        Organization          @relation(fields: [organizationId], references: [id])
  stores               stores                @relation(fields: [storeId], references: [id])

  @@index([organizationId])
}

model stock_count_lines {
  id                   Int                  @id @default(autoincrement())
  sessionId            Int
  itemId               Int
  batchId              Int?
  bookQty              Decimal
  countedQty           Decimal?
  recountQty           Decimal?
  varianceValue        Decimal?
  approvedById         String?
  item_batches         item_batches?        @relation(fields: [batchId], references: [id])
  item_master          item_master          @relation(fields: [itemId], references: [id])
  stock_count_sessions stock_count_sessions @relation(fields: [sessionId], references: [id])
}

model stock_count_sessions {
  id                Int                 @id @default(autoincrement())
  storeId           Int
  sessionRef        String              @unique
  scopeFilter       Json?
  status            String              @default("DRAFT")
  blindCount        Boolean             @default(false)
  approvedById      String?
  postedAt          DateTime?
  organizationId    String
  createdAt         DateTime            @default(now())
  stock_adjustments stock_adjustments[]
  stock_count_lines stock_count_lines[]
  organizations     Organization        @relation(fields: [organizationId], references: [id])
  stores            stores              @relation(fields: [storeId], references: [id])

  @@index([organizationId])
}

model stock_issue_items {
  id           Int           @id @default(autoincrement())
  issueId      Int
  itemId       Int
  batchId      Int?
  quantity     Decimal
  unitCost     Decimal
  item_batches item_batches? @relation(fields: [batchId], references: [id])
  stock_issues stock_issues  @relation(fields: [issueId], references: [id])
  item_master  item_master   @relation(fields: [itemId], references: [id])
}

model stock_issues {
  id                Int                 @id @default(autoincrement())
  issueNumber       String              @unique
  indentId          Int?
  fromStoreId       Int
  toStoreId         Int?
  costCenter        String?
  issuedById        String
  status            String              @default("DRAFT")
  organizationId    String
  createdAt         DateTime            @default(now())
  stock_issue_items stock_issue_items[]
  stores            stores              @relation(fields: [fromStoreId], references: [id])
  indents           indents?            @relation(fields: [indentId], references: [id])
  users             User                @relation(fields: [issuedById], references: [id])
  organizations     Organization        @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model stock_transfer_items {
  id              Int             @id @default(autoincrement())
  transferId      Int
  itemId          Int
  batchId         Int?
  quantity        Decimal
  unitCost        Decimal
  item_batches    item_batches?   @relation(fields: [batchId], references: [id])
  item_master     item_master     @relation(fields: [itemId], references: [id])
  stock_transfers stock_transfers @relation(fields: [transferId], references: [id])
}

model stock_transfers {
  id                                         Int                    @id @default(autoincrement())
  transferNumber                             String                 @unique
  fromStoreId                                Int
  toStoreId                                  Int
  status                                     String                 @default("DRAFT")
  dispatchedAt                               DateTime?
  receivedAt                                 DateTime?
  dispatchedById                             String?
  receivedById                               String?
  organizationId                             String
  createdAt                                  DateTime               @default(now())
  stock_transfer_items                       stock_transfer_items[]
  stores_stock_transfers_fromStoreIdTostores stores                 @relation("stock_transfers_fromStoreIdTostores", fields: [fromStoreId], references: [id])
  organizations                              Organization           @relation(fields: [organizationId], references: [id])
  stores_stock_transfers_toStoreIdTostores   stores                 @relation("stock_transfers_toStoreIdTostores", fields: [toStoreId], references: [id])

  @@index([organizationId])
}

model store_item_settings {
  id           Int         @id @default(autoincrement())
  storeId      Int
  itemId       Int
  parLevel     Decimal?
  reorderPoint Decimal?
  maxLevel     Decimal?
  autoIndent   Boolean     @default(false)
  item_master  item_master @relation(fields: [itemId], references: [id])
  stores       stores      @relation(fields: [storeId], references: [id])

  @@unique([storeId, itemId])
}

model store_stock {
  id               Int           @id @default(autoincrement())
  storeId          Int
  itemId           Int
  batchId          Int?
  quantityOnHand   Decimal       @default(0)
  quantityReserved Decimal       @default(0)
  avgUnitCost      Decimal       @default(0)
  binLocation      String?
  organizationId   String
  item_batches     item_batches? @relation(fields: [batchId], references: [id])
  item_master      item_master   @relation(fields: [itemId], references: [id])
  organizations    Organization  @relation(fields: [organizationId], references: [id])
  stores           stores        @relation(fields: [storeId], references: [id])

  @@unique([storeId, itemId, batchId])
  @@index([organizationId])
  @@index([storeId, itemId])
}

model stores {
  id                                                  Int                     @id @default(autoincrement())
  storeCode                                           String
  name                                                String
  storeType                                           String
  branchId                                            String?
  parentStoreId                                       Int?
  costCenter                                          String?
  inchargeUserId                                      String?
  isActive                                            Boolean                 @default(true)
  organizationId                                      String
  createdAt                                           DateTime                @default(now())
  updatedAt                                           DateTime
  goods_receipt_notes                                 GoodsReceiptNote[]
  indents_indents_fromStoreIdTostores                 indents[]               @relation("indents_fromStoreIdTostores")
  indents_indents_toStoreIdTostores                   indents[]               @relation("indents_toStoreIdTostores")
  inventory_movements                                 inventory_movements[]
  purchase_orders                                     PurchaseOrder[]
  purchase_requisitions                               purchase_requisitions[]
  stock_adjustments                                   stock_adjustments[]
  stock_count_sessions                                stock_count_sessions[]
  stock_issues                                        stock_issues[]
  stock_transfers_stock_transfers_fromStoreIdTostores stock_transfers[]       @relation("stock_transfers_fromStoreIdTostores")
  stock_transfers_stock_transfers_toStoreIdTostores   stock_transfers[]       @relation("stock_transfers_toStoreIdTostores")
  store_item_settings                                 store_item_settings[]
  store_stock                                         store_stock[]
  branches                                            Branch?                 @relation(fields: [branchId], references: [id])
  users                                               User?                   @relation(fields: [inchargeUserId], references: [id])
  organizations                                       Organization            @relation(fields: [organizationId], references: [id])
  stores                                              stores?                 @relation("storesTostores", fields: [parentStoreId], references: [id])
  other_stores                                        stores[]                @relation("storesTostores")

  @@unique([storeCode, organizationId])
  @@index([organizationId])
>>>>>>> 58626468b555649f9a66f5e7d0acf47e87b54d6c
}
